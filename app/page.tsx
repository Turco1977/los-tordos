"use client";
import { useState, useEffect, useMemo, useCallback } from "react";
import dynamic from "next/dynamic";
import { createClient } from "@/lib/supabase/client";
import { T, AREAS, DEPTOS, ROLES, ST, SC, PST, FREQ, fn, isOD, BOOK_FAC, RENTAL_APPROVERS, TESORERO } from "@/lib/constants";
import { exportCSV, exportPDF, exportICal, exportMinutaPDF, exportMinutaWord, exportReportPDF, exportProjectPDF, exportAuditPDF } from "@/lib/export";
import { openManual } from "@/lib/manual";
import { useTheme, darkCSS } from "@/lib/theme";
import { ThemeCtx } from "@/lib/theme-context";
import { Toast, useMobile, Btn, Card, Ring, Badge, Bread } from "@/components/ui";
import { taskToDB, presuToDB, presuFromDB, provFromDB } from "@/lib/mappers";
import { useDataStore } from "@/lib/store";
import { useOfflineData } from "@/lib/use-offline";
import { Login } from "@/components/main/Login";
import { ChangePw } from "@/components/main/ChangePw";
import { SB } from "@/components/main/Sidebar";
import { MyDash, EnlaceInventario } from "@/components/main/MyDash";
import { Det } from "@/components/main/Det";
import { TList } from "@/components/main/TaskList";
import { KPIs } from "@/components/main/KPIs";
import { DeptCircles } from "@/components/main/DeptCircles";
import { Org } from "@/components/main/Org";
import { DMView } from "@/components/main/DMView";
import { NP } from "@/components/main/NewPedido";
import { Proyecto } from "@/components/main/Proyecto";
import { Profs } from "@/components/main/Profs";
import PresView from "@/components/main/PresView";
import CalView from "@/components/main/CalView";
import { CustomDash } from "@/components/main/CustomDash";
import { ProyectosView } from "@/components/main/ProyectosView";
import { KanbanView } from "@/components/main/KanbanView";
import { ActivityFeed } from "@/components/main/ActivityFeed";
import { CommView } from "@/components/main/CommView";
import { CommReq } from "@/components/main/CommReq";
import { CommandPalette } from "@/components/main/CommandPalette";
import { RecurrentTasks } from "@/components/main/RecurrentTasks";
import { ErrorBoundary } from "@/components/ErrorBoundary";
import { notifyMentioned } from "@/lib/mentions";

// Dynamic imports for heavy views — only loaded when navigated to
const SponsorsView = dynamic(() => import("@/components/main/SponsorsView").then(m => ({ default: m.SponsorsView })), { ssr: false });
const InventarioView = dynamic(() => import("@/components/main/InventarioView").then(m => ({ default: m.InventarioView })), { ssr: false });
const ReservasView = dynamic(() => import("@/components/main/ReservasView").then(m => ({ default: m.ReservasView })), { ssr: false });
const Reuniones = dynamic(() => import("@/components/main/Reuniones").then(m => ({ default: m.Reuniones })), { ssr: false });
const ArchivosView = dynamic(() => import("@/components/main/ArchivosView").then(m => ({ default: m.ArchivosView })), { ssr: false });
const NewFactura = dynamic(() => import("@/components/main/NewFactura").then(m => ({ default: m.NewFactura })), { ssr: false });
const ViajesManager = dynamic(() => import("@/components/main/ViajesManager"), { ssr: false });
const TorneosView = dynamic(() => import("@/components/main/TorneosView").then(m => ({ default: m.TorneosView })), { ssr: false });
const FixturesView = dynamic(() => import("@/components/main/FixturesView").then(m => ({ default: m.FixturesView })), { ssr: false });
const BecasView = dynamic(() => import("@/components/main/BecasView").then(m => ({ default: m.BecasView })), { ssr: false });
const AtencionSocioView = dynamic(() => import("@/components/main/AtencionSocioView").then(m => ({ default: m.AtencionSocioView })), { ssr: false });

// Custom hooks
import { useAuth } from "@/hooks/useAuth";
import { useViews } from "@/hooks/useViews";
import { useSearch } from "@/hooks/useSearch";
import { useKeyboard } from "@/hooks/useKeyboard";
import { useTaskHandlers } from "@/hooks/useTaskHandlers";
import { useNotifications } from "@/hooks/useNotifications";
import { useDataFetch } from "@/hooks/useDataFetch";
import { useRealtimeSetup } from "@/hooks/useRealtimeSetup";
import { useRecurringTasks } from "@/hooks/useRecurringTasks";
import { useCommandPalette } from "@/hooks/useCommandPalette";
import { NotificationPanel } from "@/components/main/NotificationPanel";
import { AppHeader } from "@/components/main/AppHeader";

const supabase = createClient();
const TODAY = new Date().toISOString().slice(0, 10);

/* KPI filter helpers */
const KPIF: { [k: string]: { l: string; i: string; c: string } } = { ok: { l: "Completadas", i: "✅", c: T.gn }, pend: { l: "Pendientes", i: "🔴", c: T.rd }, venc: { l: "Vencidas", i: "⏰", c: "#DC2626" }, gasto: { l: "Con Gasto", i: "💰", c: T.pr } };
const kpiFilter = (peds: any[], k: string) => { switch (k) { case "ok": return peds.filter(p => p.st === ST.OK); case "pend": return peds.filter(p => p.st === ST.P); case "venc": return peds.filter(p => p.st !== ST.OK && isOD(p.fReq)); case "gasto": return peds.filter(p => p.monto); default: return peds; } };

export default function App() {
  const mob = useMobile();
  const { mode: themeMode, toggle: toggleTheme, colors, isDark, cardBg, headerBg } = useTheme();
  const [toast, sToast] = useState<{ msg: string; type: "ok" | "err" } | null>(null);
  const showT = useCallback((msg: string, type: "ok" | "err" = "ok") => sToast({ msg, type }), []);

  // Auth
  const auth = useAuth();
  const { user, authChecked, showPw, sShowPw, logout, getToken, isAd, isSA, isPersonal } = auth;

  // Offline
  const offline = useOfflineData(user);
  const { offlineState, loadFromCache, saveToCache, sync, cacheLoaded } = offline;

  // Views
  const views = useViews(!!isPersonal);
  const { vw, sVw, prevVw, sel, sSl, aA, sAA, aD, sAD, dTasks, sDTasks, sbCol, sSbCol, sbOpen, sSbOpen, kpiFilt, sKpiFilt, preAT, sPreAT, mainRef, scrollTop, hAC, hDC, computeViewFilter, computeNav, areas, deptos } = views;

  // Search
  const search = useSearch();
  const { search: searchTerm, sSr, gsOpen, sGsOpen, gsRef, gsResults } = search;

  // Notifications
  const notifs = useNotifications(user, getToken);
  const { sendNotif, refreshNotifs, shNot, sShNot, computedNts, unreadDb, badgeCount, ntGrouped, ntColor, pushEnabled, requestPush, notifTotal, notifFilter, sNotifFilter, notifPage, sNotifPage, shNotifPrefs, sShNotifPrefs, NOTIF_LIMIT } = notifs;

  // Task handlers
  const tasks = useTaskHandlers(user, showT, sendNotif);
  const { addLog, handleBulk, handleImport, sanitize } = tasks;

  // Command palette
  const cmd = useCommandPalette(user, !!isPersonal, isDark, toggleTheme, sVw, sAA, sAD, sKpiFilt, sShowPw, sSl, logout);
  const { cmdOpen, sCmdOpen, cmdItems } = cmd;

  // Data fetching
  const data = useDataFetch(user, showT, saveToCache, loadFromCache, cacheLoaded);
  const { dataLoading, fetchAll } = data;

  // Keyboard shortcuts
  useKeyboard({ user, cmdOpen, sel, showPw, isPersonal: !!isPersonal, sVw, sCmdOpen, sAA, sAD, sKpiFilt });

  // Realtime subscriptions
  useRealtimeSetup(offlineState.isOnline, user, refreshNotifs);

  // Recurring tasks
  useRecurringTasks(user, dataLoading, fetchAll);

  // Store access
  const { users, om, peds, hitos, presu, provs, reminders, projects, projTasks, taskTemplates, projBudgets, inventory, invMaint, invDist, bookings, sponsors, sponMsgs, sponDeliveries, archivos, dbNotifs, viajes, rentalConfig, dmMsgs, dmPeer, torneos, torneoHitos, torneoClubes, fixtures, minutas, becas, asCasos, tarifario, sponContracts, sponPipeline, sponContactos, sponPropuestas, sponPropVotos, sponPropMsgs, hospInvitaciones, sponMateriales, sUs, sOm, sPd, sHi, sAgs, sMins, sPr, sPv, sRems, sProjects, sProjTasks, sTaskTemplates, sProjBudgets, sInventory, sInvMaint, sInvDist, sBookings, sSponsors, sSponMsgs, sSponDeliveries, sArchivos, sDbNotifs, sViajes, sRentalConfig, sDmMsgs, sDmPeer, sTorneos, sTorneoHitos, sTorneoClubes, sFixtures, sBecas, sAsCasos, sTarifario, sSponContracts, sSponPipeline, sSponContactos, sSponPropuestas, sSponPropVotos, sSponPropMsgs, sHospInvitaciones, sSponMateriales, sponPagos, sSponPagos } = useDataStore();

  // Tesorero identification
  const isTesorero = user ? (user.n === TESORERO.first_name && user.a === TESORERO.last_name) : false;

  // Navigation target for deep-links from notifications
  const [navTarget, sNavTarget] = useState<string|null>(null);

  // Canje usage per sponsor
  const canjeUsado = useMemo(() => { const m: Record<number, number> = {}; presu.forEach((pr: any) => { if (pr.is_canje && pr.sponsor_id && pr.status === "aprobado") { m[pr.sponsor_id] = (m[pr.sponsor_id] || 0) + Number(pr.monto || 0); } }); sponDeliveries.forEach((d: any) => { if (d.sponsor_id) { m[d.sponsor_id] = (m[d.sponsor_id] || 0) + Number(d.total_value || 0); } }); return m; }, [presu, sponDeliveries]);

  // Expose audit PDF export
  useEffect(() => { (window as any).exportAuditPDF = () => exportAuditPDF(users); return () => { delete (window as any).exportAuditPDF; }; }, [users]);

  // Auto-mark section notifications as read when navigating
  useEffect(() => { if (!vw || !user || !dbNotifs?.length) return; const unread = dbNotifs.filter((n: any) => !n.read && n.link && n.link.split(":")[0] === vw); if (unread.length === 0) return; const ids = unread.map((n: any) => n.id); sDbNotifs((prev: any) => prev.map((n: any) => ids.includes(n.id) ? { ...n, read: true } : n)); supabase.from("notifications").update({ read: true }).in("id", ids).then(() => {}); }, [vw]);

  // ── EARLY RETURNS ──
  if (!authChecked) return <div style={{ minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center", background: colors.g1 }}><div style={{ fontSize: 14, color: colors.g4 }}>Cargando...</div></div>;
  if (!user) return <Login onLogin={(u: any) => auth.sU(u)} mob={mob} />;

  // Computed values (after early returns is OK — not hooks)
  const nav = computeNav();
  const { vT, vI, vC, vP } = computeViewFilter(peds);
  const userLevel = ROLES[user.role]?.lv || 0;
  const userAreaIds = user?.dId ? DEPTOS.filter((d: any) => d.id === user.dId).map((d: any) => d.aId) : [];
  const isCdOrSe = userAreaIds.includes(100) || userAreaIds.includes(101);
  const canSeeBecasAS = isSA || isAd || isCdOrSe || user.dId === 40 || user.dId === 76;

  if (isPersonal && vw === "dash") { setTimeout(() => sVw("my"), 0); return null; }
  if (vw === "manual") { setTimeout(() => { openManual(); sVw("dash"); }, 0); return null; }

  return (
    <ErrorBoundary>
    <ThemeCtx.Provider value={{ colors, isDark, cardBg }}>
    <style dangerouslySetInnerHTML={{ __html: darkCSS }} />
    <a href="#main-content" style={{ position: "absolute", left: -9999, top: "auto", width: 1, height: 1, overflow: "hidden", zIndex: 9999 }} onFocus={e => { e.currentTarget.style.position = "fixed"; e.currentTarget.style.left = "8px"; e.currentTarget.style.top = "8px"; e.currentTarget.style.width = "auto"; e.currentTarget.style.height = "auto"; e.currentTarget.style.overflow = "visible"; e.currentTarget.style.padding = "8px 16px"; e.currentTarget.style.background = colors.nv; e.currentTarget.style.color = "#fff"; e.currentTarget.style.borderRadius = "8px"; e.currentTarget.style.fontSize = "13px"; e.currentTarget.style.fontWeight = "700"; e.currentTarget.style.textDecoration = "none"; }} onBlur={e => { e.currentTarget.style.position = "absolute"; e.currentTarget.style.left = "-9999px"; e.currentTarget.style.width = "1px"; e.currentTarget.style.height = "1px"; e.currentTarget.style.overflow = "hidden"; }}>Ir al contenido principal</a>
    <div style={{ display: "flex", minHeight: "100vh", background: colors.g1, fontFamily: "-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif", color: colors.nv }}>
      <SB aA={aA} aD={aD} onAC={hAC} onDC={hDC} col={sbCol} onCol={() => sSbCol(!sbCol)} isPersonal={isPersonal} mob={mob} sbOpen={sbOpen} onClose={() => sSbOpen(false)} vw={vw} onNav={(v: string) => { sVw(v); scrollTop(); }} user={user} />
      <div style={{ flex: 1, display: "flex", flexDirection: "column" as const, minWidth: 0 }}>
        <AppHeader user={user} mob={mob} isDark={isDark} nav={nav} vw={vw} sVw={sVw} scrollTop={scrollTop} sAA={sAA} sAD={sAD} sKpiFilt={sKpiFilt} sSbOpen={sSbOpen} search={searchTerm} sSr={sSr} gsOpen={gsOpen} sGsOpen={sGsOpen} gsRef={gsRef} gsResults={gsResults} sSl={sSl} badgeCount={badgeCount} shNot={shNot} sShNot={sShNot} notifFilter={notifFilter} sNotifFilter={sNotifFilter} notifPage={notifPage} sNotifPage={sNotifPage} refreshNotifs={refreshNotifs} toggleTheme={toggleTheme} sShowPw={sShowPw} logout={logout} offlineState={offlineState} onSync={async () => { const r = await sync(); if (r.processed > 0) showT(r.processed + " cambio" + (r.processed > 1 ? "s" : "") + " sincronizado" + (r.processed > 1 ? "s" : "")); }} headerBg={headerBg} />
        <main id="main-content" ref={mainRef} style={{ flex: 1, padding: mob ? "12px 8px" : "20px 16px", overflowY: "auto" as const, marginTop: 4 }}>
          {dataLoading ? <div style={{ display: "flex", flexDirection: "column" as const, gap: 12, padding: 16 }}>{[1, 2, 3, 4].map(i => <div key={i} style={{ background: cardBg, borderRadius: 14, padding: 18, border: "1px solid " + colors.g2 }}><div style={{ height: 12, width: i % 2 ? "60%" : "40%", background: colors.g2, borderRadius: 6, marginBottom: 10 }} /><div style={{ height: 8, width: "80%", background: colors.g2, borderRadius: 4, marginBottom: 6 }} /><div style={{ height: 8, width: "50%", background: colors.g2, borderRadius: 4 }} /></div>)}</div> : <>
          {vw === "my" && isPersonal && <MyDash user={user} onSel={(p: any) => sSl(p)} mob={mob} search={searchTerm}
            onUpdDist={async (id: number, d: any) => { try { sInvDist(prev => prev.map(x => x.id === id ? { ...x, ...d } : x)); await supabase.from("inventory_distributions").update(d).eq("id", id); showT("Actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onNotifyAdmin={(title: string, msg: string) => { const admins = users.filter((u: any) => u.role === "admin" || u.role === "superadmin" || u.role === "coordinador"); admins.forEach((a: any) => sendNotif(a.id, title, msg, "info")); }}
            onConfirmReceipt={async (id: number) => { try { const now = new Date().toISOString(); sInvDist(prev => prev.map(x => x.id === id ? { ...x, received: true, received_at: now } : x)); await supabase.from("inventory_distributions").update({ received: true, received_at: now }).eq("id", id); const dist = invDist.find((x: any) => x.id === id); if (dist) { const itemName = inventory.find((i: any) => i.id === dist.inventory_id)?.name || "Material"; const admins = users.filter((u: any) => u.role === "admin" || u.role === "superadmin" || u.role === "coordinador"); admins.forEach((a: any) => sendNotif(a.id, "Recepción confirmada", `${fn(user)} confirmó recepción de ${dist.qty_given} ${itemName} (${dist.division})`, "info")); } showT("Recepción confirmada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
          />}
          {vw === "tasks" && !isPersonal && (() => { const myPeds = isAd ? peds : user.role === "coordinador" ? peds.filter((p: any) => p.dId === user.dId || p.cId === user.id || p.asTo === user.id || (isTesorero && p.rG && p.st === ST.E)) : user.role === "embudo" ? peds.filter((p: any) => p.rG || p.st === ST.E || p.cId === user.id || p.asTo === user.id) : peds.filter((p: any) => p.cId === user.id || p.asTo === user.id); const tot = myPeds.length, pe = myPeds.filter((p: any) => p.st === ST.P).length, cu = myPeds.filter((p: any) => [ST.C, ST.E, ST.V].indexOf(p.st) >= 0).length, ok = myPeds.filter((p: any) => p.st === ST.OK).length; const pePct = tot ? Math.round(pe / tot * 100) : 0, cuPct = tot ? Math.round(cu / tot * 100) : 0, okPct = tot ? Math.round(ok / tot * 100) : 0; return <div>
            <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 16 }}><div><h2 style={{ margin: 0, fontSize: mob ? 16 : 19, color: colors.nv, fontWeight: 800 }}>📋 {isAd ? "Todas las tareas" : "Mis tareas"}</h2><p style={{ margin: 0, fontSize: 11, color: colors.g4 }}>{tot} tareas</p></div><Btn v="p" s="s" onClick={() => sVw("new")}>+ Tarea</Btn></div>
            <div style={{ display: "flex", justifyContent: "center", gap: mob ? 24 : 40, marginBottom: 20 }}>
              <div style={{ textAlign: "center" as const }}><Ring pct={pePct} color={colors.rd} size={mob ? 70 : 80} /><div style={{ fontSize: 11, fontWeight: 700, color: colors.rd, marginTop: 4 }}>Pendientes</div><div style={{ fontSize: 13, fontWeight: 800, color: colors.nv }}>{pe}</div></div>
              <div style={{ textAlign: "center" as const }}><Ring pct={cuPct} color={colors.yl} size={mob ? 70 : 80} /><div style={{ fontSize: 11, fontWeight: 700, color: colors.yl, marginTop: 4 }}>En curso</div><div style={{ fontSize: 13, fontWeight: 800, color: colors.nv }}>{cu}</div></div>
              <div style={{ textAlign: "center" as const }}><Ring pct={okPct} color={colors.gn} size={mob ? 70 : 80} /><div style={{ fontSize: 11, fontWeight: 700, color: colors.gn, marginTop: 4 }}>Completadas</div><div style={{ fontSize: 13, fontWeight: 800, color: colors.nv }}>{ok}</div></div>
            </div>
            <TList title="Tareas" icon="📋" color={T.nv} peds={myPeds} onSel={(p: any) => sSl(p)} search={searchTerm} mob={mob} onBulk={handleBulk} onImport={handleImport} user={user} />
          </div>; })()}
          {vw === "kanban" && !isPersonal && <KanbanView user={user} onSel={(p: any) => sSl(p)} mob={mob} onStatusChange={async (id: number, newSt: string) => { try { sPd(p => p.map(x => x.id === id ? { ...x, st: newSt } : x)); await supabase.from("tasks").update({ status: newSt }).eq("id", id); addLog(id, user.id, fn(user), "Cambió estado a " + (SC[newSt]?.l || newSt), "sys"); const p2 = peds.find(x => x.id === id); if (p2?.asTo && p2.asTo !== user.id) sendNotif(p2.asTo, "Tarea #" + id + " → " + (SC[newSt]?.l || newSt), (p2.tit || p2.desc || "").slice(0, 60), "task", "task:" + id); if (p2?.cId && p2.cId !== user.id && p2.cId !== p2.asTo) sendNotif(p2.cId, "Tarea #" + id + " → " + (SC[newSt]?.l || newSt), (p2.tit || p2.desc || "").slice(0, 60), "task", "task:" + id); showT("Estado actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); } }} />}
          {vw === "feed" && !isPersonal && <ActivityFeed onSel={(p: any) => sSl(p)} mob={mob} />}
          {vw === "comm" && (isAd || user.role === "coordinador") && <CommView user={user} mob={mob} />}
          {vw === "recurrentes" && (isAd || user.role === "coordinador") && <RecurrentTasks user={user} mob={mob} onBack={() => sVw("new")}
            onAdd={async (d: any) => { try { const { data, error } = await supabase.from("task_templates").insert(d).select().single(); if (error) throw new Error(error.message); if (data) sTaskTemplates(prev => [data, ...prev]); showT("Template creado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpd={async (id: number, d: any) => { try { sTaskTemplates(prev => prev.map(t => t.id === id ? { ...t, ...d } : t)); await supabase.from("task_templates").update(d).eq("id", id); showT("Template actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDel={async (id: number) => { try { sTaskTemplates(prev => prev.filter(t => t.id !== id)); await supabase.from("task_templates").delete().eq("id", id); showT("Template eliminado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
          />}
          {vw === "inventario" && (isAd || user.role === "coordinador") && <InventarioView user={user} mob={mob}
            onAdd={async (d: any) => { try { const { data, error } = await supabase.from("inventory").insert(d).select().single(); if (error) throw new Error(error.message); if (data) sInventory(prev => [data, ...prev]); showT("Item agregado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpd={async (id: number, d: any) => { try { sInventory(prev => prev.map(x => x.id === id ? { ...x, ...d } : x)); await supabase.from("inventory").update(d).eq("id", id); showT("Item actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDel={async (id: number) => { try { sInventory(prev => prev.filter(x => x.id !== id)); await supabase.from("inventory").delete().eq("id", id); showT("Item eliminado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onBulkAdd={async (rows: any[]) => { try { const { data, error } = await supabase.from("inventory").insert(rows).select(); if (error) throw new Error(error.message); if (data) sInventory(prev => [...data, ...prev]); showT(data?.length + " items importados"); } catch (e: any) { showT(e.message || "Error al importar", "err"); } }}
            onAddMaint={async (d: any) => { try { const { data, error } = await supabase.from("inventory_maintenance").insert(d).select().single(); if (error) throw new Error(error.message); if (data) sInvMaint(prev => [data, ...prev]); if (d.next_due) { await supabase.from("inventory").update({ next_maint_date: d.next_due }).eq("id", d.inventory_id); sInventory(prev => prev.map(x => x.id === d.inventory_id ? { ...x, next_maint_date: d.next_due } : x)); } showT("Mantenimiento registrado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpdMaint={async (id: number, d: any) => { try { sInvMaint(prev => prev.map(x => x.id === id ? { ...x, ...d } : x)); await supabase.from("inventory_maintenance").update(d).eq("id", id); showT("Mantenimiento actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelMaint={async (id: number) => { try { sInvMaint(prev => prev.filter(x => x.id !== id)); await supabase.from("inventory_maintenance").delete().eq("id", id); showT("Registro eliminado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onAddDist={async (d: any) => { try { const { data, error } = await supabase.from("inventory_distributions").insert(d).select().single(); if (error) throw new Error(error.message); if (data) sInvDist(prev => [data, ...prev]); if (d.enlace_id) { const itemName = inventory.find((i: any) => i.id === d.inventory_id)?.name || "Material"; sendNotif(d.enlace_id, "Te entregaron material", `${d.qty_given} ${itemName} para ${d.division}`, "info"); } showT("Distribución registrada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpdDist={async (id: number, d: any) => { try { sInvDist(prev => prev.map(x => x.id === id ? { ...x, ...d } : x)); await supabase.from("inventory_distributions").update(d).eq("id", id); showT("Distribución actualizada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelDist={async (id: number) => { try { sInvDist(prev => prev.filter(x => x.id !== id)); await supabase.from("inventory_distributions").delete().eq("id", id); showT("Distribución eliminada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
          />}
          {vw === "inventario" && (user.role === "enlace" || user.role === "manager") && !isAd && user.role !== "coordinador" && <EnlaceInventario user={user} mob={mob}
            onUpdDist={async (id: number, d: any) => { try { sInvDist(prev => prev.map(x => x.id === id ? { ...x, ...d } : x)); await supabase.from("inventory_distributions").update(d).eq("id", id); showT("Actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onNotifyAdmin={(title: string, msg: string) => { const admins = users.filter((u: any) => u.role === "admin" || u.role === "superadmin" || u.role === "coordinador"); admins.forEach((a: any) => sendNotif(a.id, title, msg, "info")); }}
            onConfirmReceipt={async (id: number) => { try { const now = new Date().toISOString(); sInvDist(prev => prev.map(x => x.id === id ? { ...x, received: true, received_at: now } : x)); await supabase.from("inventory_distributions").update({ received: true, received_at: now }).eq("id", id); const dist = invDist.find((x: any) => x.id === id); if (dist) { const itemName = inventory.find((i: any) => i.id === dist.inventory_id)?.name || "Material"; const admins = users.filter((u: any) => u.role === "admin" || u.role === "superadmin" || u.role === "coordinador"); admins.forEach((a: any) => sendNotif(a.id, "Recepción confirmada", `${fn(user)} confirmó recepción de ${dist.qty_given} ${itemName} (${dist.division})`, "info")); } showT("Recepción confirmada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
          />}
          {vw === "reservas" && <ReservasView user={user} mob={mob}
            onAdd={async (d: any) => { try { const items = Array.isArray(d) ? d : [d]; const { data, error } = await supabase.from("bookings").insert(items).select(); if (error) throw new Error(error.message); if (data) sBookings(prev => [...data, ...prev]); showT(items.length > 1 ? `${items.length} espacios reservados` : "Espacio reservado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpd={async (id: number, d: any) => { try { sBookings(prev => prev.map(x => x.id === id ? { ...x, ...d } : x)); await supabase.from("bookings").update(d).eq("id", id); showT("Espacio actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDel={async (id: number) => { try { sBookings(prev => prev.filter(x => x.id !== Number(id))); await supabase.from("bookings").delete().eq("id", id); showT("Espacio eliminado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelMulti={async (ids: string[]) => { try { const numIds = ids.map(Number); sBookings(prev => prev.filter(x => !numIds.includes(x.id))); await supabase.from("bookings").delete().in("id", numIds); showT(`${ids.length} espacios eliminados`); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpdMulti={async (ids: string[], d: any) => { try { const numIds = ids.map(Number); sBookings(prev => prev.map(x => numIds.includes(x.id) ? { ...x, ...d } : x)); await supabase.from("bookings").update(d).in("id", numIds); showT(`${ids.length} espacios actualizados`); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onAddRental={async (d: any) => {
              try {
                const { data, error } = await supabase.from("bookings").insert(d).select().single();
                if (error) throw new Error(error.message);
                if (data) sBookings(prev => [data, ...prev]);
                const dow = new Date(data.date + "T12:00:00").getDay();
                const isFriSat = dow === 5 || dow === 6;
                const target = isFriSat ? { n: "Victoria", a: "Brandi" } : { n: "Lucía", a: "Gil" };
                const approver = users.find((u: any) => u.n === target.n && u.a === target.a);
                if (approver) sendNotif(approver.id, "Nuevo alquiler para aprobar", `${BOOK_FAC[data.facility]?.l || data.facility} - ${data.renter_name} - ${data.date}`, "info", "reservas");
                showT("Alquiler registrado");
              } catch (e: any) { showT(e.message || "Error", "err"); }
            }}
            onUpdRental={async (id: number, d: any) => {
              try {
                sBookings(prev => prev.map(x => x.id === id ? { ...x, ...d } : x));
                await supabase.from("bookings").update(d).eq("id", id);
                const booking = bookings.find((b: any) => b.id === id);
                const facName = BOOK_FAC[booking?.facility]?.l || booking?.facility;
                if (d.rental_status === "pendiente_pago" && booking?.booked_by) {
                  sendNotif(booking.booked_by, "Espacio disponible — pendiente pago", `${facName} - ${booking.date}`, "info", "reservas");
                }
                if (d.rental_status === "no_disponible" && booking?.booked_by) {
                  sendNotif(booking.booked_by, "Espacio no disponible", `${facName} - ${booking.date}`, "info", "reservas");
                }
                if (d.rental_status === "pago_recibido") {
                  const bp = users.find((u: any) => u.n === "Bautista" && u.a === "Pontis");
                  if (bp) sendNotif(bp.id, "Alquiler pendiente aprobación final", `${facName} - ${booking?.renter_name} - ${booking?.date}`, "info", "reservas");
                }
                if (d.rental_status === "aprobado" && booking?.booked_by) {
                  sendNotif(booking.booked_by, "Alquiler aprobado por Coordinación", `${facName} - ${booking.date}`, "info", "reservas");
                }
                if (d.rental_status === "rechazado" && booking?.booked_by) {
                  sendNotif(booking.booked_by, "Alquiler rechazado", `${facName} - ${booking.date}`, "info", "reservas");
                }
                showT("Actualizado");
              } catch (e: any) { showT(e.message || "Error", "err"); }
            }}
            onUpdRentalConfig={async (id: number, d: any) => {
              try {
                sRentalConfig(prev => prev.map(x => x.id === id ? { ...x, ...d } : x));
                await supabase.from("rental_config").update({ ...d, updated_at: new Date().toISOString() }).eq("id", id);
                showT("Tarifa actualizada");
              } catch (e: any) { showT(e.message || "Error", "err"); }
            }}
          />}
          {vw === "sponsors" && (isAd || user.role === "coordinador" || user.role === "embudo") && <SponsorsView user={user} mob={mob} canjeUsado={canjeUsado} sponMsgs={sponMsgs} sponDeliveries={sponDeliveries} navTarget={navTarget} onNavDone={()=>sNavTarget(null)}
            onAdd={async (d: any) => { const row = { ...d, created_by: user.id, created_by_name: fn(user) }; const { data, error } = await supabase.from("sponsors").insert(row).select().single(); if (error) throw new Error(error.message); if (data) { sSponsors(prev => [data, ...prev]); showT("Sponsor agregado"); } return data; }}
            onUpd={async (id: number, d: any) => { try { sSponsors(prev => prev.map(x => x.id === id ? { ...x, ...d } : x)); await supabase.from("sponsors").update(d).eq("id", id); showT("Sponsor actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDel={async (id: number) => { try { sSponsors(prev => prev.filter(x => x.id !== id)); await supabase.from("sponsors").delete().eq("id", id); showT("Sponsor eliminado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onSponMsg={async (sponsorId: number, txt: string) => { try { const safe = sanitize(txt); if (!safe) return; const ts = new Date().toISOString(); const opt = { id: Date.now(), sponsor_id: sponsorId, user_id: user.id, user_name: fn(user), content: safe, type: "msg", created_at: ts }; sSponMsgs(p => [...p, opt]); await supabase.from("sponsor_messages").insert({ sponsor_id: sponsorId, user_id: user.id, user_name: fn(user), content: safe, type: "msg" }); notifyMentioned(safe, users, user.id, "Te mencionaron en sponsor", safe.slice(0, 80), sendNotif, "info", "sponsors:" + sponsorId); } catch { showT("Error al enviar mensaje", "err"); } }}
            onAddDelivery={async (d: any) => { try { const total = Number(d.quantity || 1) * Number(d.unit_value || 0); const row = { ...d, total_value: total }; const { data, error } = await supabase.from("sponsor_deliveries").insert(row).select().single(); if (error) throw new Error(error.message); if (!data) return; sSponDeliveries(prev => [data, ...prev]); if (d.destination === "division" || d.destination === "venta") { const sp = sponsors.find((s: any) => s.id === d.sponsor_id); const invRow = { name: d.description, category: "Material Deportivo", item_type: "lote", quantity: d.quantity || 1, condition: "bueno", location: "Club", notes: "Entrega sponsor: " + (sp?.name || ""), created_by: user.id, created_by_name: fn(user) }; const { data: invData } = await supabase.from("inventory").insert(invRow).select().single(); if (invData) { sInventory(prev => [invData, ...prev]); await supabase.from("sponsor_deliveries").update({ inventory_id: invData.id }).eq("id", data.id); sSponDeliveries(prev => prev.map(x => x.id === data.id ? { ...x, inventory_id: invData.id } : x)); if (d.destination === "division" && d.division) { const distRow = { inventory_id: invData.id, division: d.division, qty_given: d.quantity || 1, distributed_by: user.id, distributed_by_name: fn(user), notes: "Auto: entrega sponsor " + (sp?.name || "") }; const { data: distData } = await supabase.from("inventory_distributions").insert(distRow).select().single(); if (distData) sInvDist(prev => [distData, ...prev]); } } } showT("Entrega registrada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpdDelivery={async (id: number, d: any) => { try { sSponDeliveries(prev => prev.map(x => x.id === id ? { ...x, ...d } : x)); await supabase.from("sponsor_deliveries").update(d).eq("id", id); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onAddTarifa={async (d: any) => { try { const { data, error } = await supabase.from("tarifario").insert(d).select().single(); if (error) throw new Error(error.message); if (data) { sTarifario(prev => [...prev, data]); showT("Tarifa agregada"); } } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpdTarifa={async (id: number, d: any) => { try { sTarifario(prev => prev.map(x => x.id === id ? { ...x, ...d } : x)); await supabase.from("tarifario").update(d).eq("id", id); showT("Tarifa actualizada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelTarifa={async (id: number) => { try { sTarifario(prev => prev.filter(x => x.id !== id)); await supabase.from("tarifario").delete().eq("id", id); showT("Tarifa eliminada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onAddContract={async (d: any) => { try { const { data, error } = await supabase.from("sponsor_contracts").insert({ ...d, created_by: user.id }).select().single(); if (error) throw new Error(error.message); if (data) { sSponContracts(prev => [data, ...prev]); showT("Contrato creado"); } } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpdContract={async (id: number, d: any) => { try { sSponContracts(prev => prev.map(x => x.id === id ? { ...x, ...d } : x)); await supabase.from("sponsor_contracts").update(d).eq("id", id); showT("Contrato actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelContract={async (id: number) => { try { sSponContracts(prev => prev.filter(x => x.id !== id)); await supabase.from("sponsor_contracts").delete().eq("id", id); showT("Contrato eliminado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onAddPipeline={async (d: any) => { try { const { data, error } = await supabase.from("sponsor_pipeline").insert({ ...d, created_by: user.id }).select().single(); if (error) throw new Error(error.message); if (data) { sSponPipeline(prev => [data, ...prev]); showT("Deal agregado"); } return data; } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpdPipeline={async (id: number, d: any) => { try { sSponPipeline(prev => prev.map(x => x.id === id ? { ...x, ...d } : x)); await supabase.from("sponsor_pipeline").update(d).eq("id", id); showT("Deal actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelPipeline={async (id: number) => { try { sSponPipeline(prev => prev.filter(x => x.id !== id)); await supabase.from("sponsor_pipeline").delete().eq("id", id); showT("Deal eliminado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onAddContacto={async (d: any) => { try { const { data, error } = await supabase.from("sponsor_contactos").insert(d).select().single(); if (error) throw new Error(error.message); if (data) { sSponContactos(prev => [data, ...prev]); showT("Contacto agregado"); } } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpdContacto={async (id: number, d: any) => { try { sSponContactos(prev => prev.map(x => x.id === id ? { ...x, ...d } : x)); await supabase.from("sponsor_contactos").update(d).eq("id", id); showT("Contacto actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelContacto={async (id: number) => { try { sSponContactos(prev => prev.filter(x => x.id !== id)); await supabase.from("sponsor_contactos").delete().eq("id", id); showT("Contacto eliminado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onAddPropuesta={async (d: any) => { try { const { data, error } = await supabase.from("sponsor_propuestas").insert({ ...d, creado_por: user.id, creado_por_name: fn(user) }).select().single(); if (error) throw new Error(error.message); if (data) { sSponPropuestas(prev => [data, ...prev]); showT("Propuesta creada"); } return data; } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpdPropuesta={async (id: number, d: any) => { try { sSponPropuestas(prev => prev.map(x => x.id === id ? { ...x, ...d } : x)); await supabase.from("sponsor_propuestas").update(d).eq("id", id); showT("Propuesta actualizada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelPropuesta={async (id: number) => { try { sSponPropuestas(prev => prev.filter(x => x.id !== id)); await supabase.from("sponsor_propuestas").delete().eq("id", id); showT("Propuesta eliminada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onAddPropVoto={async (d: any) => { try { const { data, error } = await supabase.from("sponsor_propuestas_votos").insert({ ...d, user_id: user.id, user_name: fn(user) }).select().single(); if (error) throw new Error(error.message); if (data) { sSponPropVotos(prev => [...prev, data]); showT("Voto registrado"); } } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onAddPropMsg={async (propId: number, txt: string) => { try { const safe = sanitize(txt); if (!safe) return; const opt = { id: Date.now(), propuesta_id: propId, user_id: user.id, user_name: fn(user), content: safe, created_at: new Date().toISOString() }; sSponPropMsgs(p => [...p, opt]); await supabase.from("sponsor_propuestas_mensajes").insert({ propuesta_id: propId, user_id: user.id, user_name: fn(user), content: safe }); } catch { showT("Error al enviar mensaje", "err"); } }}
            onAddHosp={async (d: any) => { try { const { data, error } = await supabase.from("hospitalidad_invitaciones").insert({ ...d, created_by: user.id }).select().single(); if (error) throw new Error(error.message); if (data) { sHospInvitaciones(prev => [data, ...prev]); showT("Invitación creada"); } } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpdHosp={async (id: number, d: any) => { try { sHospInvitaciones(prev => prev.map(x => x.id === id ? { ...x, ...d } : x)); await supabase.from("hospitalidad_invitaciones").update(d).eq("id", id); showT("Invitación actualizada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelHosp={async (id: number) => { try { sHospInvitaciones(prev => prev.filter(x => x.id !== id)); await supabase.from("hospitalidad_invitaciones").delete().eq("id", id); showT("Invitación eliminada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onAddMaterial={async (d: any) => { try { const { data, error } = await supabase.from("sponsor_materiales").insert({ ...d, subido_por: user.id, subido_por_name: fn(user) }).select().single(); if (error) throw new Error(error.message); if (data) { sSponMateriales(prev => [data, ...prev]); showT("Material subido"); } } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpdMaterial={async (id: number, d: any) => { try { sSponMateriales(prev => prev.map(x => x.id === id ? { ...x, ...d } : x)); await supabase.from("sponsor_materiales").update(d).eq("id", id); showT("Material actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelMaterial={async (id: number) => { try { sSponMateriales(prev => prev.filter(x => x.id !== id)); await supabase.from("sponsor_materiales").delete().eq("id", id); showT("Material eliminado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            sponPagos={sponPagos}
            onAddPago={async (d: any) => { try { const { data, error } = await supabase.from("sponsor_pagos").insert({ ...d, registrado_por: user.id }).select().single(); if (error) throw new Error(error.message); if (data) { sSponPagos(prev => [data, ...prev]); showT("Pago registrado"); } } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpdPago={async (id: number, d: any) => { try { sSponPagos(prev => prev.map(x => x.id === id ? { ...x, ...d } : x)); await supabase.from("sponsor_pagos").update(d).eq("id", id); showT("Pago actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelPago={async (id: number) => { try { sSponPagos(prev => prev.filter(x => x.id !== id)); await supabase.from("sponsor_pagos").delete().eq("id", id); showT("Pago eliminado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            sendNotif={sendNotif}
          />}
          {vw === "dm" && <DMView user={user} users={users} dmMsgs={dmMsgs} sDmMsgs={sDmMsgs} dmPeer={dmPeer} sDmPeer={sDmPeer} mob={mob} onSendDm={async (peerId: string, txt: string) => { try { const safe = sanitize(txt); if (!safe) return; const ts = new Date().toISOString(); const opt = { id: Date.now(), sender_id: user.id, sender_name: fn(user), receiver_id: peerId, content: safe, type: "msg", read: false, created_at: ts }; sDmMsgs((p: any[]) => [...p, opt]); await supabase.from("dm_messages").insert({ sender_id: user.id, sender_name: fn(user), receiver_id: peerId, content: safe, type: "msg" }); sendNotif(peerId, "Mensaje de " + fn(user), safe.slice(0, 80), "info", "dm"); const mentionRx = /@([\w\s]+?)(?=\s@|$)/g; let match; while ((match = mentionRx.exec(safe)) !== null) { const mName = match[1].trim(); const mUser = users.find((u: any) => (fn(u)).toLowerCase() === mName.toLowerCase()); if (mUser && mUser.id !== user.id && mUser.id !== peerId) { sendNotif(mUser.id, "Te mencionaron en un DM", safe.slice(0, 80), "info", "dm"); } } } catch { showT("Error al enviar mensaje", "err"); } }} onDeleteDm={async (msgId: number) => { try { sDmMsgs((p: any[]) => p.filter(m => m.id !== msgId)); await supabase.from("dm_messages").delete().eq("id", msgId); } catch { showT("Error al borrar mensaje", "err"); } }} onDeleteConvo={async (peerId: string) => { try { const ids = dmMsgs.filter((m: any) => (m.sender_id === user.id && m.receiver_id === peerId) || (m.sender_id === peerId && m.receiver_id === user.id)).map((m: any) => m.id); sDmMsgs((p: any[]) => p.filter(m => !ids.includes(m.id))); await supabase.from("dm_messages").delete().in("id", ids); showT("Conversacion eliminada"); } catch { showT("Error al borrar conversacion", "err"); } }} />}
          {vw === "becas" && !isPersonal && canSeeBecasAS && <BecasView user={user} mob={mob} showT={showT} becas={becas} users={users}
            onAdd={async (d: any) => { try { const { data, error } = await supabase.from("becas").insert({ ...d, responsable_id: user.id }).select().single(); if (error) throw new Error(error.message); if (data) sBecas(prev => [data, ...prev]); showT("Beca registrada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpd={async (id: number, d: any) => { try { const prev = becas.find((x: any) => x.id === id); sBecas(p => p.map(x => x.id === id ? { ...x, ...d } : x)); await supabase.from("becas").update(d).eq("id", id); if (d.estado === "deliberacion" && prev?.estado !== "deliberacion") { const seDeptIds = DEPTOS.filter((dp: any) => dp.aId === 101).map((dp: any) => dp.id); const seUsers = users.filter((u: any) => seDeptIds.includes(u.dId) || u.role === "superadmin" || u.role === "admin"); seUsers.forEach((u: any) => { if (u.id !== user.id) sendNotif(u.id, "Beca pendiente de tu voto", (prev?.nombre_completo || "Beca") + " requiere aprobacion SE", "info", "becas"); }); } showT("Beca actualizada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDel={async (id: number) => { try { sBecas(prev => prev.filter(x => x.id !== id)); await supabase.from("becas").delete().eq("id", id); showT("Beca eliminada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onVote={async (id: number) => { try { const b = becas.find((x: any) => x.id === id); if (!b) return; const votos = b.votos || []; if (votos.some((v: any) => v.userId === user.id)) { showT("Ya votaste esta beca", "err"); return; } const newVotos = [...votos, { userId: user.id, userName: fn(user), date: TODAY }]; const newEstado = newVotos.length >= 3 ? "aprobada" : b.estado; sBecas(p => p.map(x => x.id === id ? { ...x, votos: newVotos, estado: newEstado } : x)); const upd: any = { votos: newVotos }; if (newEstado !== b.estado) upd.estado = newEstado; await supabase.from("becas").update(upd).eq("id", id); showT(newEstado === "aprobada" ? "Beca aprobada por quorum" : "Voto registrado (" + newVotos.length + "/3)"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
          />}
          {vw === "atencion-socio" && !isPersonal && canSeeBecasAS && <AtencionSocioView user={user} mob={mob} showT={showT} casos={asCasos} becasAprobadas={becas.filter((b: any) => b.estado === "aprobada")} users={users}
            onAddCaso={async (d: any) => { try { const { data, error } = await supabase.from("atencion_socio_casos").insert({ ...d, responsable_id: user.id }).select().single(); if (error) throw new Error(error.message); if (data) sAsCasos(prev => [data, ...prev]); showT("Caso registrado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpdCaso={async (id: number, d: any) => { try { const prev = asCasos.find((x: any) => x.id === id); sAsCasos(p => p.map(x => x.id === id ? { ...x, ...d } : x)); await supabase.from("atencion_socio_casos").update(d).eq("id", id); if (d.estado === "deliberacion_se" && prev?.estado !== "deliberacion_se") { const seDeptIds = DEPTOS.filter((dp: any) => dp.aId === 101).map((dp: any) => dp.id); const seUsers = users.filter((u: any) => seDeptIds.includes(u.dId) || u.role === "superadmin" || u.role === "admin"); seUsers.forEach((u: any) => { if (u.id !== user.id) sendNotif(u.id, "Caso pendiente de tu voto", (prev?.nombre_socio || "Caso") + " requiere aprobacion SE", "info", "atencion-socio"); }); } showT("Caso actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelCaso={async (id: number) => { try { sAsCasos(prev => prev.filter(x => x.id !== id)); await supabase.from("atencion_socio_casos").delete().eq("id", id); showT("Caso eliminado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onVote={async (id: number) => { try { const c = asCasos.find((x: any) => x.id === id); if (!c) return; const votos = c.votos || []; if (votos.some((v: any) => v.userId === user.id)) { showT("Ya votaste este caso", "err"); return; } const newVotos = [...votos, { userId: user.id, userName: fn(user), date: TODAY }]; const newEstado = newVotos.length >= 3 ? "aprobada_se" : c.estado; sAsCasos(p => p.map(x => x.id === id ? { ...x, votos: newVotos, estado: newEstado } : x)); const upd: any = { votos: newVotos }; if (newEstado !== c.estado) upd.estado = newEstado; await supabase.from("atencion_socio_casos").update(upd).eq("id", id); showT(newEstado === "aprobada_se" ? "Caso aprobado por quorum SE" : "Voto registrado (" + newVotos.length + "/3)"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
          />}
          {vw === "archivos" && !isPersonal && <ArchivosView user={user} mob={mob} showT={showT} />}
          {vw === "viajes" && (isAd || user.role === "coordinador") && <ViajesManager viajes={viajes} userId={user.id} userLevel={ROLES[user.role]?.lv || 0} onRefresh={async () => { const { data } = await supabase.from("viajes").select("*").order("created_at", { ascending: false }); if (data) sViajes(() => data); }} mob={mob} />}
          {vw === "proyectos" && <ProyectosView user={user} mob={mob} filteredProjects={projects}
            onAddProject={async (p: any) => { try { const row = { name: p.name, description: p.description || "", created_by: user.id, created_by_name: fn(user), status: p.status || "borrador" }; const { data, error } = await supabase.from("projects").insert(row).select().single(); if (error) throw new Error(error.message); if (data) sProjects(prev => [data, ...prev]); if (p.status === "enviado") { const cdDeptIds = DEPTOS.filter((dp: any) => dp.aId === 100).map((dp: any) => dp.id); const cdUsers = users.filter((u: any) => cdDeptIds.includes(u.dId) || u.role === "superadmin" || u.role === "admin"); cdUsers.forEach((u: any) => { if (u.id !== user.id) sendNotif(u.id, "Proyecto pendiente de tu voto", (p.name || "Proyecto") + " requiere aprobacion CD", "info", "proyectos"); }); } showT(p.status === "enviado" ? "Proyecto enviado" : "Borrador guardado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpdProject={async (id: number, d: any) => { try { sProjects(prev => prev.map(p => p.id === id ? { ...p, ...d } : p)); await supabase.from("projects").update(d).eq("id", id); if (d.status === "enviado") { const cdDeptIds = DEPTOS.filter((dp: any) => dp.aId === 100).map((dp: any) => dp.id); const cdUsers = users.filter((u: any) => cdDeptIds.includes(u.dId) || u.role === "superadmin" || u.role === "admin"); cdUsers.forEach((u: any) => { if (u.id !== user.id) sendNotif(u.id, "Proyecto pendiente de tu voto", "Proyecto requiere aprobacion CD", "info", "proyectos"); }); } showT("Proyecto actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelProject={async (id: number) => { try { sProjects(prev => prev.filter(p => p.id !== id)); sProjTasks(prev => prev.filter((t: any) => t.project_id !== id)); await supabase.from("projects").delete().eq("id", id); showT("Proyecto eliminado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onAddTask={async (t: any) => { try { const { data, error } = await supabase.from("project_tasks").insert(t).select().single(); if (error) throw new Error(error.message); if (data) sProjTasks(prev => [data, ...prev]); showT("Tarea creada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpdTask={async (id: number, d: any) => { try { sProjTasks(prev => prev.map((t: any) => t.id === id ? { ...t, ...d } : t)); await supabase.from("project_tasks").update(d).eq("id", id); showT("Tarea actualizada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelTask={async (id: number) => { try { sProjTasks(prev => prev.filter((t: any) => t.id !== id)); await supabase.from("project_tasks").delete().eq("id", id); showT("Tarea eliminada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onAddBudget={async (b: any) => { try { const { data, error } = await supabase.from("project_budgets").insert(b).select().single(); if (error) throw new Error(error.message); if (data) sProjBudgets(prev => [data, ...prev]); showT("Presupuesto agregado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpdBudget={async (id: number, d: any) => { try { sProjBudgets(prev => prev.map(b => b.id === id ? { ...b, ...d } : b)); await supabase.from("project_budgets").update(d).eq("id", id); showT("Presupuesto actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelBudget={async (id: number) => { try { sProjBudgets(prev => prev.filter(b => b.id !== id)); await supabase.from("project_budgets").delete().eq("id", id); showT("Presupuesto eliminado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onVoteProject={async (id: number) => { try { const proj = projects.find((p: any) => p.id === id); if (!proj) return; const votos = proj.votos || []; if (votos.some((v: any) => v.userId === user.id)) { showT("Ya votaste este proyecto", "err"); return; } const newVotos = [...votos, { userId: user.id, userName: fn(user), date: TODAY, vote: "aprobar" }]; const apro = newVotos.filter((v: any) => v.vote === "aprobar").length; const newStatus = apro >= 5 ? "aprobado" : proj.status; sProjects(prev => prev.map(p => p.id === id ? { ...p, votos: newVotos, status: newStatus } : p)); const upd: any = { votos: newVotos }; if (newStatus !== proj.status) upd.status = newStatus; await supabase.from("projects").update(upd).eq("id", id); showT(newStatus === "aprobado" ? "Proyecto aprobado por quorum CD (5/5)" : "Voto aprobado registrado (" + apro + "/5)"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onRejectProject={async (id: number) => { try { const proj = projects.find((p: any) => p.id === id); if (!proj) return; const votos = proj.votos || []; if (votos.some((v: any) => v.userId === user.id)) { showT("Ya votaste este proyecto", "err"); return; } const newVotos = [...votos, { userId: user.id, userName: fn(user), date: TODAY, vote: "rechazar" }]; const rech = newVotos.filter((v: any) => v.vote === "rechazar").length; const newStatus = rech >= 4 ? "rechazado" : proj.status; sProjects(prev => prev.map(p => p.id === id ? { ...p, votos: newVotos, status: newStatus } : p)); const upd: any = { votos: newVotos }; if (newStatus !== proj.status) upd.status = newStatus; await supabase.from("projects").update(upd).eq("id", id); showT(newStatus === "rechazado" ? "Proyecto rechazado por CD" : "Voto rechazado registrado (" + rech + "/4)"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
          />}
          {vw === "torneos" && !isPersonal && (isAd || user.role === "coordinador") && <TorneosView user={user} mob={mob}
            onAdd={async (d: any) => { try { const { data, error } = await supabase.from("torneos").insert(d).select().single(); if (error) throw new Error(error.message); if (data) sTorneos(prev => [data, ...prev]); showT("Torneo creado"); return data; } catch (e: any) { showT(e.message || "Error", "err"); return null; } }}
            onUpd={async (id: number, d: any) => { try { sTorneos(prev => prev.map(t => t.id === id ? { ...t, ...d } : t)); await supabase.from("torneos").update(d).eq("id", id); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDel={async (id: number) => { try { sTorneos(prev => prev.filter(t => t.id !== id)); sTorneoHitos(prev => prev.filter((h: any) => h.torneo_id !== id)); sTorneoClubes(prev => prev.filter((c: any) => c.torneo_id !== id)); await supabase.from("torneos").delete().eq("id", id); showT("Torneo eliminado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onAddHito={async (rows: any[]) => { try { const { data, error } = await supabase.from("torneo_hitos").insert(rows).select(); if (error) throw new Error(error.message); if (data) sTorneoHitos(prev => [...prev, ...data]); } catch (e: any) { showT(e.message || "Error al crear hitos", "err"); } }}
            onUpdHito={async (id: number, d: any) => { try { sTorneoHitos(prev => prev.map(h => h.id === id ? { ...h, ...d } : h)); await supabase.from("torneo_hitos").update(d).eq("id", id); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onAddClub={async (d: any) => { try { const { data, error } = await supabase.from("torneo_clubes").insert(d).select().single(); if (error) throw new Error(error.message); if (data) sTorneoClubes(prev => [data, ...prev]); showT("Club agregado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpdClub={async (id: number, d: any) => { try { sTorneoClubes(prev => prev.map(c => c.id === id ? { ...c, ...d } : c)); await supabase.from("torneo_clubes").update(d).eq("id", id); showT("Club actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelClub={async (id: number) => { try { sTorneoClubes(prev => prev.filter(c => c.id !== id)); await supabase.from("torneo_clubes").delete().eq("id", id); showT("Club eliminado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onAddPresu={async (d: any) => { try { const row = presuToDB(d); const { data, error } = await supabase.from("presupuestos").insert(row).select().single(); if (error) throw new Error(error.message); sPr(p => [presuFromDB(data), ...p]); const ctUsers = users.filter((u: any) => u.dId === 7 || u.dId === 61 || u.dId === 53 || u.role === "embudo"); ctUsers.forEach((u: any) => { if (u.id !== user.id) sendNotif(u.id, "Nuevo presupuesto pendiente", (d.proveedor_nombre || "Presupuesto") + " - $" + (Number(d.monto) || 0).toLocaleString(), "budget", "presu"); }); showT("Presupuesto agregado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onAddComm={async (p: any) => { try { const row: any = taskToDB(p); const { data, error } = await supabase.from("tasks").insert(row).select().single(); if (error) throw new Error(error.message); const tid = data?.id || p.id; const localP = { ...p, id: tid }; sPd(ps => [localP, ...ps]); for (const l of (p.log || [])) { await supabase.from("task_messages").insert({ task_id: tid, user_id: l.uid, user_name: l.by, content: l.act, type: l.t }); } if (localP.asTo && localP.asTo !== user.id) { sendNotif(localP.asTo, "Nueva tarea asignada #" + tid, (localP.tit || localP.desc || "").slice(0, 80), "task", "task:" + tid, true); } showT("Pedido de comunicación creado"); } catch (e: any) { showT(e.message || "Error al crear pedido", "err"); } }}
          />}
          {vw === "fixtures" && !isPersonal && (isAd || user.role === "coordinador") && <FixturesView user={user} mob={mob}
            onAdd={async (rows: any[]) => { try { const toInsert = rows.map((r: any) => ({ ...r, created_by: user.id, created_by_name: (user.n || "") + " " + (user.a || "") })); const { data, error } = await supabase.from("fixtures").insert(toInsert).select(); if (error) throw new Error(error.message); if (data) sFixtures(prev => [...data, ...prev]); showT(data?.length + " fixture(s) creado(s)"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpd={async (id: number, d: any) => { try { sFixtures(prev => prev.map(f => f.id === id ? { ...f, ...d } : f)); await supabase.from("fixtures").update(d).eq("id", id); showT("Fixture actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDel={async (id: number) => { try { sFixtures(prev => prev.filter(f => f.id !== id)); await supabase.from("fixtures").delete().eq("id", id); showT("Fixture eliminado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelWeek={async (weekStartDate: string) => { try { sFixtures(prev => prev.filter(f => f.week_start_date !== weekStartDate)); await supabase.from("fixtures").delete().eq("week_start_date", weekStartDate); showT("Semana eliminada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onAddBookings={async (items: any[]) => { try { for (const item of items) { const { data, error } = await supabase.from("bookings").insert(item.booking).select().single(); if (error) throw new Error(error.message); if (data) { sBookings((prev: any) => [data, ...prev]); await supabase.from("fixtures").update({ booking_id: data.id }).eq("id", item.fixtureId); sFixtures((prev: any) => prev.map((f: any) => f.id === item.fixtureId ? { ...f, booking_id: data.id } : f)); } } showT(items.length + " reserva(s) creada(s) en Espacios"); } catch (e: any) { showT(e.message || "Error al sincronizar", "err"); } }}
            onNavHosp={(f: any) => { sNavTarget("hosp:" + f.date + ":" + (f.rival || "") + ":" + f.id); sVw("sponsors"); }}
          />}
          {vw === "org" && <Org areas={areas} deptos={deptos} users={users} om={om} pedidos={peds} onSel={(p: any) => sSl(p)} onEditSave={async (id: string, d: any) => { sOm(p => p.map(m => m.id === id ? { ...m, ...d } : m)); await supabase.from("org_members").update({ first_name: d.n, last_name: d.a, email: d.mail || "", phone: d.tel || "" }).eq("id", id); }} onDelOm={async (id: string) => { sOm(p => p.filter(m => m.id !== id)); await supabase.from("org_members").delete().eq("id", id); }} onDelUser={async (id: string) => { sUs(p => p.filter(u => u.id !== id)); await supabase.from("profiles").delete().eq("id", id); }} onEditUser={() => { sVw("profs"); }} isSA={isSA} onAssignTask={(u: any) => { sPreAT(u); sVw("new"); }} onDm={(u: any) => { sDmPeer(u.id); sVw("dm"); }} mob={mob} showT={showT} userLevel={userLevel}
            onReorderOm={async (id: string, dir: string, type: string) => { const grp = om.filter((m: any) => m.t === type).sort((a: any, b: any) => (a.so || 0) - (b.so || 0)); const idx = grp.findIndex((m: any) => m.id === id); const si = dir === "up" ? idx - 1 : idx + 1; if (si < 0 || si >= grp.length) return; [grp[idx], grp[si]] = [grp[si], grp[idx]]; const upd: any = {}; grp.forEach((m: any, i: number) => { upd[m.id] = i; }); sOm(prev => prev.map((m: any) => upd[m.id] !== undefined ? { ...m, so: upd[m.id] } : m)); for (const m of grp) { await supabase.from("org_members").update({ sort_order: upd[m.id] }).eq("id", m.id); } }}
            onReorderUser={async (uid: string, dir: string, dId: number) => { const grp = users.filter((u: any) => u.dId === dId).sort((a: any, b: any) => (a.so || 0) - (b.so || 0)); const idx = grp.findIndex((u: any) => u.id === uid); const si = dir === "up" ? idx - 1 : idx + 1; if (si < 0 || si >= grp.length) return; [grp[idx], grp[si]] = [grp[si], grp[idx]]; const upd: any = {}; grp.forEach((u: any, i: number) => { upd[u.id] = i; }); sUs(prev => prev.map((u: any) => upd[u.id] !== undefined ? { ...u, so: upd[u.id] } : u)); for (const u of grp) { await supabase.from("profiles").update({ sort_order: upd[u.id] }).eq("id", u.id); } }}
          />}
          {vw === "cal" && <CalView user={user} onSel={(p: any) => sSl(p)} mob={mob} onNav={(v: string) => sVw(v)}
            onDateChange={async (id: number, newDate: string) => { try { sPd(p => p.map(x => x.id === id ? { ...x, fReq: newDate } : x)); await supabase.from("tasks").update({ due_date: newDate }).eq("id", id); addLog(id, user.id, fn(user), "Cambió fecha límite a " + newDate.split("-").reverse().join("/"), "sys"); showT("Fecha actualizada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onAddReminder={async (r: any) => { try { const row: any = { user_id: user.id, user_name: fn(user), title: r.title, date: r.date, description: r.description || "", color: r.color || "#3B82F6", recurrence: r.recurrence || "none", assigned_to: r.assigned_to || null, assigned_name: r.assigned_name || "" }; const { data, error } = await supabase.from("reminders").insert(row).select().single(); if (error) throw new Error(error.message); sRems(p => [...(data ? [data] : []), ...p]); showT("Recordatorio creado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelReminder={async (id: number) => { try { sRems(p => p.filter(x => x.id !== id)); await supabase.from("reminders").delete().eq("id", id); showT("Recordatorio eliminado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
          />}
          {vw === "presu" && (isAd || user.role === "coordinador" || user.role === "embudo") && <div>{prevVw === "cal" && <button onClick={() => sVw("cal")} style={{ display: "inline-flex", alignItems: "center", gap: 4, padding: "5px 12px", borderRadius: 8, border: "1px solid " + colors.g3, background: colors.g1, color: colors.nv, fontSize: 11, fontWeight: 600, cursor: "pointer", marginBottom: 10 }}>← Volver al Calendario</button>}<PresView user={user} mob={mob} onSel={(p: any) => sSl(p)}
            onAddPresu={async (d: any) => { try { const row = presuToDB(d); const { data, error } = await supabase.from("presupuestos").insert(row).select().single(); if (error) throw new Error(error.message); sPr(p => [presuFromDB(data), ...p]); const ctUsers = users.filter((u: any) => u.dId === 7 || u.dId === 61 || u.dId === 53 || u.role === "embudo"); ctUsers.forEach((u: any) => { if (u.id !== user.id) sendNotif(u.id, "Nuevo presupuesto pendiente", (d.proveedor_nombre || "Presupuesto") + " - $" + (Number(d.monto) || 0).toLocaleString(), "budget", "presu"); }); showT("Presupuesto agregado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onUpdPresu={async (id: number, d: any) => { try { const prev = presu.find(x => x.id === id); sPr(p => p.map(x => x.id === id ? { ...x, ...d } : x)); await supabase.from("presupuestos").update(d).eq("id", id); if (d.status && prev && d.status !== prev.status && prev.solicitado_por) { const stLabel = d.status === "aprobado" ? "aprobado" : d.status === "rechazado" ? "rechazado" : d.status === "recibido" ? "recibido" : "actualizado"; const reqUser = users.find((u: any) => fn(u) === prev.solicitado_por || u.id === prev.solicitado_por); if (reqUser && reqUser.id !== user.id) sendNotif(reqUser.id, "Presupuesto " + stLabel, prev.descripcion?.slice(0, 60) || "", "budget", prev.task_id ? "task:" + prev.task_id : "presu"); } showT("Presupuesto actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelPresu={async (id: number) => { try { sPr(p => p.filter(x => x.id !== id)); await supabase.from("presupuestos").delete().eq("id", id); showT("Presupuesto eliminado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onAddProv={async (d: any) => { try { const { data, error } = await supabase.from("proveedores").insert(d).select().single(); if (error) throw new Error(error.message); sPv(p => [provFromDB(data), ...p]); showT("Proveedor agregado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onVotePresu={async (id: number, vote: "aprobar" | "rechazar") => { try { const pr = presu.find((x: any) => x.id === id); if (!pr) return; const votos = pr.votos || []; if (votos.some((v: any) => v.userId === user.id)) { showT("Ya votaste este presupuesto", "err"); return; } const tipo = (user.dId === 61 || user.dId === 53) ? "tesoreria" : (user.dId === 7 || user.role === "embudo") ? "compras" : userAreaIds.includes(101) ? "se" : "compras"; const newVotos = [...votos, { userId: user.id, userName: fn(user), date: TODAY, vote, tipo }]; if (vote === "rechazar") { sPr(p => p.map(x => x.id === id ? { ...x, votos: newVotos, status: "rechazado" } : x)); await supabase.from("presupuestos").update({ votos: newVotos, status: "rechazado" }).eq("id", id); if (pr.solicitado_por) { const reqUser = users.find((u: any) => fn(u) === pr.solicitado_por || u.id === pr.solicitado_por); if (reqUser && reqUser.id !== user.id) sendNotif(reqUser.id, "Presupuesto rechazado", (pr.proveedor_nombre || "") + " - $" + Number(pr.monto).toLocaleString(), "budget", "presu"); } showT("Presupuesto rechazado"); return; } const comprasOk = newVotos.filter((v: any) => v.tipo === "compras" && v.vote === "aprobar").length >= 1; const tesOk = newVotos.filter((v: any) => v.tipo === "tesoreria" && v.vote === "aprobar").length >= 1; let newStatus = pr.status; if (pr.status === "solicitado" && comprasOk && tesOk) { newStatus = (pr.monto > 2000000 && pr.moneda === "ARS") ? "pendiente_se" : "aprobado"; } if (pr.status === "pendiente_se") { const seOk = newVotos.filter((v: any) => v.tipo === "se" && v.vote === "aprobar").length >= 3; if (seOk) newStatus = "aprobado"; } sPr(p => p.map(x => x.id === id ? { ...x, votos: newVotos, status: newStatus } : x)); const upd: any = { votos: newVotos }; if (newStatus !== pr.status) upd.status = newStatus; await supabase.from("presupuestos").update(upd).eq("id", id); if (newStatus === "pendiente_se") { const seDeptIds = DEPTOS.filter((d: any) => d.aId === 101).map((d: any) => d.id); users.filter((u: any) => seDeptIds.includes(u.dId)).forEach((u: any) => { if (u.id !== user.id) sendNotif(u.id, "Presupuesto >$2M pendiente SE", (pr.proveedor_nombre || "") + " - $" + Number(pr.monto).toLocaleString(), "budget", "presu"); }); } if (newStatus === "aprobado" && pr.solicitado_por) { const reqUser = users.find((u: any) => fn(u) === pr.solicitado_por || u.id === pr.solicitado_por); if (reqUser && reqUser.id !== user.id) sendNotif(reqUser.id, "Presupuesto aprobado", (pr.proveedor_nombre || "") + " - $" + Number(pr.monto).toLocaleString(), "budget", "presu"); } showT(newStatus === "aprobado" ? "Presupuesto aprobado" : newStatus === "pendiente_se" ? "Aprobado por Compras/Tesorería — pendiente SE" : "Voto registrado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
          /></div>}
          {vw === "reun" && (isAd || user.role === "coordinador") && <div>{prevVw === "cal" && <button onClick={() => sVw("cal")} style={{ display: "inline-flex", alignItems: "center", gap: 4, padding: "5px 12px", borderRadius: 8, border: "1px solid " + colors.g3, background: colors.g1, color: colors.nv, fontSize: 11, fontWeight: 600, cursor: "pointer", marginBottom: 10 }}>← Volver al Calendario</button>}<Reuniones user={user} mob={mob}
            onAddAg={async (a: any) => { try { const { data, error } = await supabase.from("agendas").insert({ type: a.type, area_name: a.areaName || null, date: a.date, sections: a.sections, presentes: a.presentes || [], status: a.status, created_at: a.createdAt || TODAY }).select().single(); if (error) throw new Error(error.message); const nid = data?.id || 0; sAgs(p => [{ ...a, id: nid }, ...p]); showT(a.status === "enviada" ? "OD enviada y minuta creada" : "OD guardada"); return nid; } catch (e: any) { showT(e.message || "Error al guardar OD", "err"); return 0; } }}
            onUpdAg={async (id: number, d: any) => { try { sAgs(p => p.map(a => a.id === id ? { ...a, ...d, areaName: d.area_name !== undefined ? d.area_name : a.areaName } : a)); const dbUpd: any = {}; if (d.status) dbUpd.status = d.status; if (d.sections) dbUpd.sections = d.sections; if (d.presentes) dbUpd.presentes = d.presentes; if (d.date) dbUpd.date = d.date; if (d.area_name !== undefined) dbUpd.area_name = d.area_name; await supabase.from("agendas").update(dbUpd).eq("id", id); showT("OD actualizada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelAg={async (id: number) => { try { sAgs(p => p.filter(a => a.id !== id)); await supabase.from("agendas").delete().eq("id", id); showT("OD eliminada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onAddMin={async (m: any) => { try { const { data } = await supabase.from("minutas").insert({ type: m.type, area_name: m.areaName || null, agenda_id: m.agendaId || null, date: m.date, hora_inicio: m.horaInicio, hora_cierre: m.horaCierre, lugar: m.lugar, presentes: m.presentes, ausentes: m.ausentes, sections: m.sections, tareas: m.tareas, status: m.status, created_at: m.createdAt || TODAY }).select().single(); if (data) sMins(p => [{ ...m, id: data.id }, ...p]); else sMins(p => [m, ...p]); showT(m.status === "final" ? "Minuta finalizada" : "Minuta guardada"); const allText = ((m.sections || []).map((s: any) => s.content || "").join(" ") + " " + (m.tareas || []).map((t: any) => t.desc || "").join(" ")); notifyMentioned(allText, users, user.id, "Te mencionaron en una minuta", fn(user) + " te mencionó en una minuta", sendNotif, "info", "reun"); } catch (e: any) { showT(e.message || "Error al guardar minuta", "err"); } }}
            onUpdMin={async (id: number, d: any) => { try { const silent = d._silent; const { _silent, ...cleanD } = d; sMins(p => p.map(m => m.id === id ? { ...m, ...cleanD } : m)); const upd: any = {}; if (cleanD.status) upd.status = cleanD.status; if (cleanD.sections) upd.sections = cleanD.sections; if (cleanD.tareas) upd.tareas = cleanD.tareas; if (cleanD.presentes) upd.presentes = cleanD.presentes; if (cleanD.ausentes) upd.ausentes = cleanD.ausentes; if (cleanD.date) upd.date = cleanD.date; if (cleanD.hora_inicio !== undefined) upd.hora_inicio = cleanD.hora_inicio; if (cleanD.hora_cierre !== undefined) upd.hora_cierre = cleanD.hora_cierre; if (cleanD.lugar !== undefined) upd.lugar = cleanD.lugar; if (cleanD.approvals) upd.approvals = cleanD.approvals; await supabase.from("minutas").update(upd).eq("id", id); if (!silent) { showT("Minuta actualizada"); const allText = ((cleanD.sections || []).map((s: any) => s.content || "").join(" ") + " " + (cleanD.tareas || []).map((t: any) => t.desc || "").join(" ")); notifyMentioned(allText, users, user.id, "Te mencionaron en una minuta", fn(user) + " te mencionó en una minuta actualizada", sendNotif, "info", "reun"); } } catch (e: any) { if (!d._silent) showT(e.message || "Error", "err"); } }}
            onApproveMin={async (id: number) => { try { const mi = minutas.find((m: any) => m.id === id); if (!mi) return; const apps = mi.approvals || []; if (apps.some((a: any) => a.userId === user.id)) { showT("Ya votaste esta minuta", "err"); return; } const newApps = [...apps, { userId: user.id, userName: fn(user), date: TODAY, vote: "aprobar" }]; const quorum = mi.type === "cd" ? 8 : mi.type === "se" ? 3 : 999; const allVoted = newApps.length >= quorum; const approved = allVoted && newApps.filter((a: any) => a.vote === "aprobar").length > newApps.filter((a: any) => a.vote === "rechazar").length; const newStatus = allVoted ? (approved ? "aprobada" : "rechazada") : mi.status; sMins(p => p.map(m => m.id === id ? { ...m, approvals: newApps, status: newStatus } : m)); const upd: any = { approvals: newApps }; if (newStatus !== mi.status) upd.status = newStatus; await supabase.from("minutas").update(upd).eq("id", id); showT(allVoted ? (approved ? "Minuta aprobada por mayoría" : "Minuta rechazada por mayoría") : "Voto registrado (" + newApps.length + "/" + quorum + ")"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onRejectMin={async (id: number) => { try { const mi = minutas.find((m: any) => m.id === id); if (!mi) return; const apps = mi.approvals || []; if (apps.some((a: any) => a.userId === user.id)) { showT("Ya votaste esta minuta", "err"); return; } const newApps = [...apps, { userId: user.id, userName: fn(user), date: TODAY, vote: "rechazar" }]; const quorum = mi.type === "cd" ? 8 : mi.type === "se" ? 3 : 999; const allVoted = newApps.length >= quorum; const approved = allVoted && newApps.filter((a: any) => a.vote === "aprobar").length > newApps.filter((a: any) => a.vote === "rechazar").length; const newStatus = allVoted ? (approved ? "aprobada" : "rechazada") : mi.status; sMins(p => p.map(m => m.id === id ? { ...m, approvals: newApps, status: newStatus } : m)); const upd: any = { approvals: newApps }; if (newStatus !== mi.status) upd.status = newStatus; await supabase.from("minutas").update(upd).eq("id", id); showT(allVoted ? (approved ? "Minuta aprobada por mayoría" : "Minuta rechazada por mayoría") : "Rechazo registrado (" + newApps.length + "/" + quorum + ")"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onDelMin={async (id: number) => { try { sMins(p => p.filter(m => m.id !== id)); await supabase.from("minutas").delete().eq("id", id); showT("Minuta eliminada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
            onCreateTasks={async (tareas: any[]) => { try { const ts = TODAY + " " + new Date().toTimeString().slice(0, 5); const newTasks: any[] = []; for (const t of tareas) { const resp = t.respId ? users.find((u: any) => u.id === t.respId) : null; const tit = (t.desc || "").slice(0, 100); const msgs: any[] = [{ task_id: 0, user_id: user.id, user_name: fn(user), content: "Creó tarea desde minuta", type: "sys" }]; if (resp) msgs.push({ task_id: 0, user_id: user.id, user_name: fn(user), content: "Asignó a " + fn(resp), type: "sys" }); const row: any = { division: "", creator_id: user.id, creator_name: fn(user), dept_id: resp?.dId || user.dId || 1, tipo: "Administrativo", title: tit, description: t.desc, due_date: t.fecha || "", urgency: "Normal", status: "curso", assigned_to: t.respId || null, requires_expense: false, expense_ok: null, resolution: "", created_at: TODAY, amount: null }; const { data } = await supabase.from("tasks").insert(row).select().single(); const tid = data?.id || 0; if (tid) { const dbMsgs = msgs.map(m => ({ ...m, task_id: tid })); await supabase.from("task_messages").insert(dbMsgs); if (resp && t.respId !== user.id) sendNotif(t.respId, "Nueva tarea asignada #" + tid, tit.slice(0, 80), "task", "task:" + tid, true); newTasks.push({ id: tid, div: "", cId: user.id, cN: fn(user), dId: resp?.dId || user.dId || 1, tipo: "Administrativo", tit, desc: t.desc, fReq: t.fecha || "", urg: "Normal", st: ST.C, asTo: t.respId || null, rG: false, eOk: null, resp: "", cAt: TODAY, monto: null, log: dbMsgs.map(m => ({ dt: ts, uid: m.user_id, by: m.user_name, act: m.content, t: m.type })) }); } } sPd(p => [...newTasks, ...p]); showT(newTasks.length + " tarea(s) creada(s)"); } catch (e: any) { showT(e.message || "Error al crear tareas", "err"); } }}
          /></div>}
          {vw === "profs" && <Profs onDel={async (id: string) => { try { sUs(p => p.filter(u => u.id !== id)); await supabase.from("profiles").delete().eq("id", id); showT("Perfil eliminado"); } catch (e: any) { showT(e.message || "Error", "err"); } }} onAdd={async (u: any) => {
            try { const { data: { session } } = await supabase.auth.getSession(); const tok = session?.access_token;
            if (!tok || !u.mail) { sUs(p => [...p, u]); showT("Perfil creado (sin cuenta auth)"); return; }
            const res = await fetch("/api/admin/create-user", { method: "POST", headers: { "Content-Type": "application/json", "Authorization": "Bearer " + tok }, body: JSON.stringify({ email: u.mail, first_name: u.n, last_name: u.a, role: u.role, dept_id: u.dId, division: u.div, phone: u.tel }) });
            const json = await res.json(); if (json.error) { showT(json.error, "err"); return; } if (json.user) { sUs(p => [...p, { ...u, id: json.user.id }]); showT("Usuario creado correctamente"); } else { sUs(p => [...p, u]); showT("Perfil creado"); }
            } catch (e: any) { showT(e.message || "Error al crear usuario", "err"); }
          }} onEditUser={async (id: string, d: any) => {
            try { sUs(p => p.map(u => u.id === id ? { ...u, ...d } : u)); await supabase.from("profiles").update({ first_name: d.n, last_name: d.a, role: d.role, dept_id: d.dId, division: d.div, email: d.mail || "", phone: d.tel || "" }).eq("id", id);
            if (d.mail) { const oldUser = users.find(u => u.id === id); if (oldUser && d.mail !== oldUser.mail) { const { data: { session } } = await supabase.auth.getSession(); const tok = session?.access_token; if (tok) await fetch("/api/admin/create-user", { method: "PUT", headers: { "Content-Type": "application/json", "Authorization": "Bearer " + tok }, body: JSON.stringify({ userId: id, email: d.mail }) }); } }
            showT("Perfil actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); }
          }} isAd={isAd} onAssignTask={(u: any) => { sPreAT(u); sVw("new"); }} mob={mob} />}
          {vw === "new" && !preAT && <Card style={{ maxWidth: mob ? undefined : 480, textAlign: "center" as const }}>
            <h2 style={{ margin: "0 0 16px", fontSize: mob ? 15 : 17, color: colors.nv, fontWeight: 800 }}>¿Qué querés crear?</h2>
            <div style={{ display: "flex", gap: 12, justifyContent: "center", flexWrap: "wrap" as const }}>
              <div onClick={() => sVw("new-task")} style={{ flex: 1, minWidth: 160, padding: mob ? 20 : 24, background: colors.nv + "08", border: "2px solid " + colors.nv, borderRadius: 14, cursor: "pointer", textAlign: "center" as const }}><div style={{ fontSize: 28, marginBottom: 8 }}>🏉</div><div style={{ fontSize: 14, fontWeight: 700, color: colors.nv }}>+ Nueva Tarea</div><div style={{ fontSize: 11, color: colors.g4, marginTop: 4 }}>Logística, infraestructura, administrativo, etc.</div></div>
              <div onClick={() => sVw("comm-req")} style={{ flex: 1, minWidth: 160, padding: mob ? 20 : 24, background: "#8B5CF608", border: "2px solid #8B5CF6", borderRadius: 14, cursor: "pointer", textAlign: "center" as const }}><div style={{ fontSize: 28, marginBottom: 8 }}>📣</div><div style={{ fontSize: 14, fontWeight: 700, color: "#8B5CF6" }}>+ Comunicación</div><div style={{ fontSize: 11, color: colors.g4, marginTop: 4 }}>Pedir flyer, post, video, placa al depto de Comunicación</div></div>
              <div onClick={() => sVw("new-factura")} style={{ flex: 1, minWidth: 160, padding: mob ? 20 : 24, background: "#05966908", border: "2px solid #059669", borderRadius: 14, cursor: "pointer", textAlign: "center" as const }}><div style={{ fontSize: 28, marginBottom: 8 }}>🧾</div><div style={{ fontSize: 14, fontWeight: 700, color: "#059669" }}>+ Factura</div><div style={{ fontSize: 11, color: colors.g4, marginTop: 4 }}>Cargar factura o remito con proveedor y monto</div></div>
              {(isAd || user.role === "coordinador") && <div onClick={() => sVw("recurrentes")} style={{ flex: 1, minWidth: 160, padding: mob ? 20 : 24, background: "#F59E0B08", border: "2px solid #F59E0B", borderRadius: 14, cursor: "pointer", textAlign: "center" as const }}><div style={{ fontSize: 28, marginBottom: 8 }}>🔁</div><div style={{ fontSize: 14, fontWeight: 700, color: "#F59E0B" }}>+ Recurrente</div><div style={{ fontSize: 11, color: colors.g4, marginTop: 4 }}>Tarea que se repite y se envía automáticamente</div></div>}
              <div onClick={() => sVw("proyectos")} style={{ flex: 1, minWidth: 160, padding: mob ? 20 : 24, background: "#3B82F608", border: "2px solid #3B82F6", borderRadius: 14, cursor: "pointer", textAlign: "center" as const }}><div style={{ fontSize: 28, marginBottom: 8 }}>📋</div><div style={{ fontSize: 14, fontWeight: 700, color: "#3B82F6" }}>+ Nuevo Proyecto</div><div style={{ fontSize: 11, color: colors.g4, marginTop: 4 }}>Presentar proyecto al CD para aprobación</div></div>
            </div>
            <div style={{ marginTop: 16 }}><Btn v="g" onClick={() => sVw(isPersonal ? "my" : "tasks")}>← Volver</Btn></div>
          </Card>}
          {(vw === "new-task" || (vw === "new" && preAT)) && <NP user={user} preAssign={preAT} preArea={aA||undefined} preDept={aD||undefined} mob={mob} canjeUsado={canjeUsado} onSub={async (p: any) => {
            try { const row: any = taskToDB(p); const { data, error } = await supabase.from("tasks").insert(row).select().single(); if (error) throw new Error(error.message); const tid = data?.id || p.id; const localP = { ...p, id: tid }; sPd(ps => [localP, ...ps]); for (const l of (p.log || [])) { await supabase.from("task_messages").insert({ task_id: tid, user_id: l.uid, user_name: l.by, content: l.act, type: l.t }); } if (localP.asTo && localP.asTo !== user.id) { sendNotif(localP.asTo, "Nueva tarea asignada #" + tid, (localP.tit || localP.desc || "").slice(0, 80), "task", "task:" + tid, true); } if (p._presu && tid) { const prRow = presuToDB({ ...p._presu, task_id: tid, status: PST.SOL, solicitado_por: fn(user), solicitado_at: TODAY }); const { data: prData } = await supabase.from("presupuestos").insert(prRow).select().single(); if (prData) sPr(prev => [presuFromDB(prData), ...prev]); } if (localP.rG) { await supabase.from("task_messages").insert({ task_id: tid, user_id: user.id, user_name: fn(user), content: "Auto-enviado a Compras y Tesorería", type: "sys" }); users.filter(u => u.role === "embudo").forEach(u => sendNotif(u.id, "Nueva tarea en Compras #" + tid, (localP.tit || localP.desc || "").slice(0, 80), "budget", "task:" + tid)); const tes = users.find(u => u.n === TESORERO.first_name && u.a === TESORERO.last_name); if (tes) sendNotif(tes.id, "Gasto pendiente aprobación #" + tid, (localP.tit || localP.desc || "").slice(0, 80), "budget", "task:" + tid, true); } sPreAT(null); sVw(isPersonal ? "my" : "tasks"); sAA(null); sAD(null); showT(p._presu ? (p._presu.is_canje ? "Tarea creada con canje" : "Tarea creada con presupuesto") : localP.rG ? "Tarea creada → Compras/Tesorería" : "Tarea creada"); } catch (e: any) { showT(e.message || "Error al crear tarea", "err"); }
          }} onX={() => { sPreAT(null); sVw(preAT ? (isPersonal ? "my" : "tasks") : "new"); }} />}
          {vw === "comm-req" && <CommReq user={user} mob={mob} onSub={async (p: any) => {
            try { const row: any = taskToDB(p); const { data, error } = await supabase.from("tasks").insert(row).select().single(); if (error) throw new Error(error.message); const tid = data?.id || p.id; const localP = { ...p, id: tid }; sPd(ps => [localP, ...ps]); for (const l of (p.log || [])) { await supabase.from("task_messages").insert({ task_id: tid, user_id: l.uid, user_name: l.by, content: l.act, type: l.t }); } sVw(isPersonal ? "my" : "tasks"); showT("Pedido de comunicación creado"); } catch (e: any) { showT(e.message || "Error al crear pedido", "err"); }
          }} onX={() => sVw("new")} />}
          {vw === "new-factura" && <NewFactura user={user} mob={mob} onSub={async (d: any) => {
            try {
              // Update the archivos record created by upload with factura metadata
              const { data: existing } = await supabase.from("archivos").select("id").eq("url", d.fileUrl).maybeSingle();
              if (existing) {
                const upd = { titulo: d.titulo, proveedor: d.proveedor, monto: d.monto, moneda: d.moneda, nro_factura: d.nro_factura || null, fecha_factura: d.fecha_factura || null, notas: d.notas || null };
                await supabase.from("archivos").update(upd).eq("id", existing.id);
                sArchivos(prev => prev.map((a: any) => a.id === existing.id ? { ...a, ...upd } : a));
              } else {
                // Fallback: insert new record if upload didn't auto-create one
                const row = { titulo: d.titulo, name: d.fileName, url: d.fileUrl, folder: "facturas", category: "factura", mime_type: d.mimeType, size_bytes: d.fileSize, uploaded_by: user.id, proveedor: d.proveedor, monto: d.monto, moneda: d.moneda, nro_factura: d.nro_factura || null, fecha_factura: d.fecha_factura || null, notas: d.notas || null };
                const { data } = await supabase.from("archivos").insert(row).select().single();
                if (data) sArchivos(prev => [data, ...prev]);
              }
              sVw("archivos"); showT("Factura guardada");
            } catch (e: any) { showT(e.message || "Error al guardar factura", "err"); }
          }} onX={() => sVw("new")} />}
          {vw === "proy" && <Proyecto setHitos={(updater: any) => { sHi((prev: any) => { const next = typeof updater === "function" ? updater(prev) : updater; next.forEach((h: any) => { supabase.from("milestones").update({ pct: h.pct }).eq("id", h.id); }); return next; }); }} isAd={isAd} mob={mob} />}
          {vw === "dash" && !isPersonal && !aA && !aD && !kpiFilt && <CustomDash user={user} mob={mob} onSel={(p: any) => sSl(p)} onFilter={(k: string) => sKpiFilt(k)} onAC={hAC} isSA={isSA} onNav={(view: string, filt?: string) => { if (view === "filter" && filt) { sKpiFilt(filt); } else { sVw(view); } }}
            onExportWeekly={() => { const today = new Date(); const weekAgo = new Date(today.getTime() - 7 * 86400000); const fmtD2 = (d: Date) => d.toISOString().slice(0, 10); const range = fmtD2(weekAgo) + " al " + fmtD2(today); const ok = peds.filter((p: any) => p.st === ST.OK); const pend = peds.filter((p: any) => p.st === ST.P); const od = peds.filter((p: any) => p.st !== ST.OK && isOD(p.fReq)); const approvedPr = presu.filter((pr: any) => pr.status === "aprobado"); const pendPr = presu.filter((pr: any) => pr.status !== "aprobado" && pr.status !== "rechazado"); const topAreas = areas.map((a: any) => { const dIds = deptos.filter((d: any) => d.aId === a.id).map((d: any) => d.id); const aP = peds.filter((p: any) => dIds.indexOf(p.dId) >= 0); const aOk = aP.filter((p: any) => p.st === ST.OK).length; return { name: a.name, total: aP.length, completed: aOk, pct: aP.length ? Math.round(aOk / aP.length * 100) : 0 }; }).filter((a: any) => a.total > 0); exportReportPDF({ period: "Semanal", dateRange: range, stats: [{ label: "Total tareas", value: String(peds.length), color: "#0A1628" }, { label: "Completadas", value: String(ok.length), color: "#10B981" }, { label: "Pendientes", value: String(pend.length), color: "#DC2626" }, { label: "Vencidas", value: String(od.length), color: "#7C3AED" }], tasksByStatus: Object.keys(SC).map(k => ({ status: SC[k].l, icon: SC[k].i, count: peds.filter((p: any) => p.st === k).length, color: SC[k].c })), completedTasks: ok.slice(0, 20).map((p: any) => { const ag = users.find((u: any) => u.id === p.asTo); return { id: p.id, desc: p.desc, assignee: ag ? fn(ag) : "–", date: p.fReq }; }), pendingTasks: [...od, ...pend].slice(0, 20).map((p: any) => { const ag = users.find((u: any) => u.id === p.asTo); return { id: p.id, desc: p.desc, assignee: ag ? fn(ag) : "–", date: p.fReq, overdue: p.st !== ST.OK && isOD(p.fReq) }; }), budgetSummary: { total: presu.reduce((s: number, p: any) => s + Number(p.monto || 0), 0), approved: approvedPr.reduce((s: number, p: any) => s + Number(p.monto || 0), 0), pending: pendPr.reduce((s: number, p: any) => s + Number(p.monto || 0), 0), currency: "ARS" }, topAreas }); }}
            onExportMonthly={() => { const today = new Date(); const monthAgo = new Date(today.getFullYear(), today.getMonth() - 1, today.getDate()); const fmtD2 = (d: Date) => d.toISOString().slice(0, 10); const range = fmtD2(monthAgo) + " al " + fmtD2(today); const ok = peds.filter((p: any) => p.st === ST.OK); const pend = peds.filter((p: any) => p.st === ST.P); const od = peds.filter((p: any) => p.st !== ST.OK && isOD(p.fReq)); const approvedPr = presu.filter((pr: any) => pr.status === "aprobado"); const pendPr = presu.filter((pr: any) => pr.status !== "aprobado" && pr.status !== "rechazado"); const topAreas = areas.map((a: any) => { const dIds = deptos.filter((d: any) => d.aId === a.id).map((d: any) => d.id); const aP = peds.filter((p: any) => dIds.indexOf(p.dId) >= 0); const aOk = aP.filter((p: any) => p.st === ST.OK).length; return { name: a.name, total: aP.length, completed: aOk, pct: aP.length ? Math.round(aOk / aP.length * 100) : 0 }; }).filter((a: any) => a.total > 0); exportReportPDF({ period: "Mensual", dateRange: range, stats: [{ label: "Total tareas", value: String(peds.length), color: "#0A1628" }, { label: "Completadas", value: String(ok.length), color: "#10B981" }, { label: "Pendientes", value: String(pend.length), color: "#DC2626" }, { label: "Vencidas", value: String(od.length), color: "#7C3AED" }], tasksByStatus: Object.keys(SC).map(k => ({ status: SC[k].l, icon: SC[k].i, count: peds.filter((p: any) => p.st === k).length, color: SC[k].c })), completedTasks: ok.slice(0, 20).map((p: any) => { const ag = users.find((u: any) => u.id === p.asTo); return { id: p.id, desc: p.desc, assignee: ag ? fn(ag) : "–", date: p.fReq }; }), pendingTasks: [...od, ...pend].slice(0, 20).map((p: any) => { const ag = users.find((u: any) => u.id === p.asTo); return { id: p.id, desc: p.desc, assignee: ag ? fn(ag) : "–", date: p.fReq, overdue: p.st !== ST.OK && isOD(p.fReq) }; }), budgetSummary: { total: presu.reduce((s: number, p: any) => s + Number(p.monto || 0), 0), approved: approvedPr.reduce((s: number, p: any) => s + Number(p.monto || 0), 0), pending: pendPr.reduce((s: number, p: any) => s + Number(p.monto || 0), 0), currency: "ARS" }, topAreas }); }}
          />}
          {vw === "dash" && !isPersonal && !aA && !aD && kpiFilt && (() => { const fl = KPIF[kpiFilt]; const fp = kpiFilter(peds, kpiFilt); return <div><Bread parts={[{ label: "Dashboard", onClick: () => sKpiFilt(null) }, { label: fl?.l || "" }]} mob={mob} /><TList title={fl?.l || ""} icon={fl?.i || ""} color={fl?.c || T.bl} peds={fp} onSel={(p: any) => sSl(p)} search={searchTerm} mob={mob} onBulk={handleBulk} onImport={handleImport} user={user} /></div>; })()}
          {vw === "dash" && !isPersonal && aA && !aD && (aA === 100 || aA === 101) && (() => { const selAr = areas.find((a: any) => a.id === aA); return <div><Bread parts={[{ label: "Dashboard", onClick: () => { sAA(null); sKpiFilt(null); } }, { label: selAr?.name || "" }]} mob={mob} /><h2 style={{ margin: "0 0 4px", fontSize: mob ? 16 : 19, color: colors.nv, fontWeight: 800 }}>{selAr?.icon} {selAr?.name}</h2><p style={{ color: colors.g4, fontSize: 12, margin: "0 0 16px" }}>{deptos.filter((d: any) => d.aId === aA).length} cargos</p><KPIs peds={vP} mob={mob} onFilter={(k: string) => { sAA(null); sKpiFilt(k); }} /><DeptCircles area={selAr} onDC={(id: number) => sAD(id)} mob={mob} /></div>; })()}
          {vw === "dash" && !isPersonal && aA && !aD && aA !== 100 && aA !== 101 && (() => { const selAr = areas.find((a: any) => a.id === aA); const rootDs = deptos.filter((d: any) => d.aId === aA && !d.pId); return <div><Bread parts={[{ label: "Dashboard", onClick: () => { sAA(null); sKpiFilt(null); } }, { label: selAr?.name || "" }]} mob={mob} /><div style={{display:"flex",alignItems:"center",justifyContent:"space-between",marginBottom:4}}><div><h2 style={{ margin: 0, fontSize: mob ? 16 : 19, color: colors.nv, fontWeight: 800 }}>{selAr?.icon} {selAr?.name}</h2><p style={{ color: colors.g4, fontSize: 12, margin: "4px 0 0" }}>{rootDs.length} departamentos</p></div><Btn v="p" s="s" onClick={() => sVw("new-task")}>+ Tarea</Btn></div><KPIs peds={vP} mob={mob} onFilter={(k: string) => { sAA(null); sKpiFilt(k); }} /><DeptCircles area={selAr} onDC={(id: number) => sAD(id)} mob={mob} /></div>; })()}
          {vw === "dash" && !isPersonal && aD && deptos.some((d: any) => d.pId === aD) && !dTasks && (() => { const selAr = areas.find((a: any) => a.id === aA); const selDp = deptos.find((d: any) => d.id === aD); const childCount = deptos.filter((d: any) => d.pId === aD).length; const deptMembers = users.filter((u: any) => u.dId === aD); return <div><Bread parts={[{ label: "Dashboard", onClick: () => { sAA(null); sAD(null); sKpiFilt(null); } }, { label: selAr?.name || "", onClick: () => { sAD(null); sKpiFilt(null); } }, { label: selDp?.name || "" }]} mob={mob} /><div style={{display:"flex",alignItems:"center",justifyContent:"space-between",marginBottom:4}}><div><h2 style={{ margin: 0, fontSize: mob ? 16 : 19, color: colors.nv, fontWeight: 800 }}>📂 {selDp?.name}</h2><p style={{ color: colors.g4, fontSize: 12, margin: "4px 0 0" }}>{childCount} sub-departamentos{deptMembers.length > 0 ? " · " + deptMembers.map((u: any) => fn(u)).join(", ") : ""}</p></div><Btn v="p" s="s" onClick={() => sVw("new-task")}>+ Tarea</Btn></div><KPIs peds={vP} mob={mob} onFilter={(k: string) => { sAA(null); sKpiFilt(k); }} /><DeptCircles area={selAr} parentId={aD} onDC={(id: number) => sAD(id)} mob={mob} onSelf={() => sDTasks(true)} /></div>; })()}
          {vw === "dash" && !isPersonal && aD && (!deptos.some((d: any) => d.pId === aD) || dTasks) && (() => { const selAr = areas.find((a: any) => a.id === aA); const selDp = deptos.find((d: any) => d.id === aD); const parentDp = selDp?.pId ? deptos.find((d: any) => d.id === selDp.pId) : null; const parts: any[] = [{ label: "Dashboard", onClick: () => { sAA(null); sAD(null); sKpiFilt(null); } }, { label: selAr?.name || "", onClick: () => { sAD(null); sKpiFilt(null); } }]; if (dTasks && deptos.some((d: any) => d.pId === aD)) { parts.push({ label: selDp?.name || "", onClick: () => sDTasks(false) }); parts.push({ label: "Tareas directas" }); } else { if (parentDp) parts.push({ label: parentDp.name, onClick: () => sAD(parentDp.id) }); parts.push({ label: selDp?.name || "" }); } const taskPeds = dTasks ? vP.filter((p: any) => p.dId === aD) : vP; return <div><Bread parts={parts} mob={mob} /><TList title={dTasks ? (selDp?.name || "") + " (directo)" : vT} icon={vI} color={vC} peds={taskPeds} onSel={(p: any) => sSl(p)} search={searchTerm} mob={mob} onBulk={handleBulk} onImport={handleImport} user={user} onNewTask={() => sVw("new-task")} /></div>; })()}
          </>}
        </main>
      </div>
      {showPw && <ChangePw onX={() => sShowPw(false)} />}
      {sel && <Det p={peds.find(x => x.id === sel.id) || sel} user={user} onX={() => sSl(null)} mob={mob}
        onDup={async (p: any) => { try { const ts = TODAY + " " + new Date().toTimeString().slice(0, 5); const row: any = taskToDB({ ...p, st: ST.P, asTo: null, resp: "", monto: null, eOk: null, cAt: TODAY, cId: user.id, cN: fn(user) }); const { data, error } = await supabase.from("tasks").insert(row).select().single(); if (error) throw new Error(error.message); const tid = data?.id || 0; const newP = { ...p, id: tid, st: ST.P, asTo: null, resp: "", monto: null, eOk: null, cAt: TODAY, cId: user.id, cN: fn(user), log: [{ dt: ts, uid: user.id, by: fn(user), act: "Creó tarea (duplicada de #" + p.id + ")", t: "sys" }] }; sPd(ps => [newP, ...ps]); await supabase.from("task_messages").insert({ task_id: tid, user_id: user.id, user_name: fn(user), content: "Creó tarea (duplicada de #" + p.id + ")", type: "sys" }); sSl(newP); showT("Tarea duplicada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
        onCheck={async (id: number, items: any[]) => { try { await supabase.from("task_messages").delete().eq("task_id", id).eq("type", "check"); if (items.length > 0) { await supabase.from("task_messages").insert(items.map(item => ({ task_id: id, user_id: user.id, user_name: fn(user), content: JSON.stringify(item), type: "check" }))); } const ts = TODAY + " " + new Date().toTimeString().slice(0, 5); sPd(p => p.map(x => x.id === id ? { ...x, log: [...(x.log || []).filter((l: any) => l.t !== "check"), ...items.map(item => ({ dt: ts, uid: user.id, by: fn(user), act: JSON.stringify(item), t: "check" }))] } : x)); const allText = items.map(it => it.text || it.label || "").join(" "); if (allText) notifyMentioned(allText, users, user.id, "Te mencionaron en checklist de tarea #" + id, fn(user) + " te mencionó en un checklist", sendNotif, "task", "task:" + id); } catch (e: any) { showT(e.message || "Error", "err"); } }}
        onAddPresu={async (d: any) => { try { const row = presuToDB(d); const { data, error } = await supabase.from("presupuestos").insert(row).select().single(); if (error) throw new Error(error.message); sPr(p => [presuFromDB(data), ...p]); const ctUsers = users.filter((u: any) => u.dId === 7 || u.dId === 61 || u.dId === 53 || u.role === "embudo"); ctUsers.forEach((u: any) => { if (u.id !== user.id) sendNotif(u.id, "Nuevo presupuesto pendiente", (d.proveedor_nombre || "Presupuesto") + " - $" + (Number(d.monto) || 0).toLocaleString(), "budget", "presu"); }); showT("Presupuesto agregado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
        onUpdPresu={async (id: number, d: any) => { try { const prev = presu.find(x => x.id === id); sPr(p => p.map(x => x.id === id ? { ...x, ...d } : x)); await supabase.from("presupuestos").update(d).eq("id", id); if (d.status && prev && d.status !== prev.status && prev.solicitado_por) { const stLabel = d.status === "aprobado" ? "aprobado" : d.status === "rechazado" ? "rechazado" : d.status === "recibido" ? "recibido" : "actualizado"; const reqUser = users.find((u: any) => fn(u) === prev.solicitado_por || u.id === prev.solicitado_por); if (reqUser && reqUser.id !== user.id) sendNotif(reqUser.id, "Presupuesto " + stLabel, prev.descripcion?.slice(0, 60) || "", "budget", prev.task_id ? "task:" + prev.task_id : "presu"); } showT("Presupuesto actualizado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
        onDelPresu={async (id: number) => { try { sPr(p => p.filter(x => x.id !== id)); await supabase.from("presupuestos").delete().eq("id", id); showT("Presupuesto eliminado"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
        onTk={async (id: number) => { try { const prev = peds.find(x => x.id === id); const prevAsTo = prev?.asTo; sPd(p => p.map(x => x.id === id ? { ...x, asTo: user.id, st: ST.C } : x)); await supabase.from("tasks").update({ assigned_to: user.id, status: ST.C }).eq("id", id); addLog(id, user.id, fn(user), "Tomó la tarea", "sys"); if (prevAsTo && prevAsTo !== user.id) sendNotif(prevAsTo, fn(user) + " tomó la tarea #" + id, (prev?.tit || prev?.desc || "").slice(0, 60), "task", "task:" + id); if (prev?.cId && prev.cId !== user.id && prev.cId !== prevAsTo) sendNotif(prev.cId, fn(user) + " tomó la tarea #" + id, (prev?.tit || prev?.desc || "").slice(0, 60), "task", "task:" + id); showT("Tarea tomada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
        onAs={async (id: number, uid: string) => { try { const ag = users.find(u => u.id === uid); const p2 = peds.find(x => x.id === id); const newSt = p2?.st === ST.P ? ST.C : p2?.st; sPd(p => p.map(x => x.id === id ? { ...x, asTo: uid, st: x.st === ST.P ? ST.C : x.st } : x)); await supabase.from("tasks").update({ assigned_to: uid, status: newSt }).eq("id", id); addLog(id, user.id, fn(user), "Asignó a " + (ag ? fn(ag) : ""), "sys"); sendNotif(uid, "Te asignaron una tarea", p2?.desc || "", "task", "task:" + id, true); showT("Tarea asignada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
        onRe={async (id: number, r: string) => { try { const sr = sanitize(r); sPd(p => p.map(x => x.id === id ? { ...x, resp: sr } : x)); await supabase.from("tasks").update({ resolution: sr }).eq("id", id); showT("Resolución guardada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
        onSE={async (id: number) => { try { sPd(p => p.map(x => x.id === id ? { ...x, st: ST.E } : x)); await supabase.from("tasks").update({ status: ST.E }).eq("id", id); addLog(id, user.id, fn(user), "Envió a Compras y Tesorería", "sys"); users.filter(u => u.role === "embudo").forEach(u => sendNotif(u.id, "Nueva tarea en Compras", peds.find(x => x.id === id)?.desc || "", "budget", "task:" + id)); const tes = users.find(u => u.n === TESORERO.first_name && u.a === TESORERO.last_name); if (tes) sendNotif(tes.id, "Gasto pendiente aprobación #" + id, peds.find(x => x.id === id)?.desc || "", "budget", "task:" + id, true); showT("Enviado a Compras y Tesorería"); sSl(null); } catch (e: any) { showT(e.message || "Error", "err"); } }}
        onEO={async (id: number, ok: boolean) => { try { const p2 = peds.find(x => x.id === id); if (!ok) { sPd(p => p.map(x => x.id === id ? { ...x, st: ST.C, eOk: false } : x)); await supabase.from("tasks").update({ status: ST.C, expense_ok: false }).eq("id", id); addLog(id, user.id, fn(user), "Compras rechazó", "sys"); } else { const bothOk = p2?.tOk === true; const newSt = bothOk ? ST.C : ST.E; sPd(p => p.map(x => x.id === id ? { ...x, st: newSt, eOk: true } : x)); await supabase.from("tasks").update({ status: newSt, expense_ok: true }).eq("id", id); addLog(id, user.id, fn(user), "Compras aprobó presupuesto", "sys"); if (bothOk) { addLog(id, user.id, fn(user), "Compra autorizada — ambas aprobaciones completas", "sys"); if (p2?.asTo) sendNotif(p2.asTo, "Compra autorizada #" + id, "Compras y Tesorería aprobaron", "budget", "task:" + id, true); } } if (p2?.asTo) sendNotif(p2.asTo, ok ? "Compras aprobó presupuesto" : "Compras rechazó gasto", p2.desc || "", "budget", "task:" + id); if (p2?.cId && p2.cId !== p2.asTo) sendNotif(p2.cId, ok ? "Compras aprobó presupuesto" : "Compras rechazó gasto", p2.desc || "", "budget", "task:" + id); showT(ok ? "Presupuesto aprobado" : "Gasto rechazado"); sSl(null); } catch (e: any) { showT(e.message || "Error", "err"); } }}
        onTO={async (id: number, ok: boolean) => { try { const p2 = peds.find(x => x.id === id); if (!ok) { sPd(p => p.map(x => x.id === id ? { ...x, st: ST.C, tOk: false } : x)); await supabase.from("tasks").update({ status: ST.C, tesoreria_ok: false }).eq("id", id); addLog(id, user.id, fn(user), "Tesorería rechazó el gasto", "sys"); } else { const bothOk = p2?.eOk === true; const newSt = bothOk ? ST.C : ST.E; sPd(p => p.map(x => x.id === id ? { ...x, st: newSt, tOk: true } : x)); await supabase.from("tasks").update({ status: newSt, tesoreria_ok: true }).eq("id", id); addLog(id, user.id, fn(user), "Tesorería aprobó el gasto", "sys"); if (bothOk) { addLog(id, user.id, fn(user), "Compra autorizada — ambas aprobaciones completas", "sys"); if (p2?.asTo) sendNotif(p2.asTo, "Compra autorizada #" + id, "Compras y Tesorería aprobaron", "budget", "task:" + id, true); } else { users.filter(u => u.role === "embudo").forEach(u => sendNotif(u.id, "Tesorería aprobó #" + id + " — pendiente presupuesto", p2?.desc || "", "budget", "task:" + id)); } } if (p2?.asTo) sendNotif(p2.asTo, ok ? "Tesorería aprobó" : "Tesorería rechazó", p2.desc || "", "budget", "task:" + id); if (p2?.cId && p2.cId !== p2.asTo) sendNotif(p2.cId, ok ? "Tesorería aprobó" : "Tesorería rechazó", p2.desc || "", "budget", "task:" + id); showT(ok ? "Aprobado por Tesorería" : "Rechazado por Tesorería"); sSl(null); } catch (e: any) { showT(e.message || "Error", "err"); } }}
        onFi={async (id: number) => { try { const p2 = peds.find(x => x.id === id); sPd(p => p.map(x => x.id === id ? { ...x, st: ST.V } : x)); await supabase.from("tasks").update({ status: ST.V }).eq("id", id); addLog(id, user.id, fn(user), "Envió a validación", "sys"); if (p2?.cId) sendNotif(p2.cId, "Tarea lista para validar", p2.desc || "", "task", "task:" + id); showT("Enviado a validación"); sSl(null); } catch (e: any) { showT(e.message || "Error", "err"); } }}
        onVa={async (id: number, ok: boolean) => { try { const p2 = peds.find(x => x.id === id); const ns = ok ? ST.OK : ST.C; sPd(p => p.map(x => x.id === id ? { ...x, st: ns } : x)); await supabase.from("tasks").update({ status: ns }).eq("id", id); addLog(id, user.id, fn(user), ok ? "Validó OK ✅" : "Rechazó", "sys"); if (p2?.asTo) sendNotif(p2.asTo, ok ? "Tarea completada" : "Tarea rechazada", p2.desc || "", "task", "task:" + id); let autoInv = false; if (ok && p2?.rG && p2?.tipo === "Material deportivo") { const ag = users.find((u: any) => u.id === p2.asTo); const { data: invD } = await supabase.from("inventory").insert({ name: p2.desc || "Material", category: "deportivo", quantity: 1, condition: "nuevo", responsible_id: p2.asTo || null, responsible_name: ag ? fn(ag) : "", notes: "Auto tarea #" + id }).select().single(); if (invD) { sInventory(prev => [invD, ...prev]); autoInv = true; } } if (ok && p2?.cId && p2.cId !== user.id) sendNotif(p2.cId, autoInv ? "Compra lista + inventariada" : "Tu pedido está listo", p2.desc || "", "task", "task:" + id); showT(ok ? (autoInv ? "Completada + Inventario" : "Tarea completada") : "Tarea rechazada"); sSl(null); } catch (e: any) { showT(e.message || "Error", "err"); } }}
        onMsg={async (id: number, txt: string) => { try { const safe = sanitize(txt); if (!safe) return; await addLog(id, user.id, fn(user), safe, "msg"); const p2 = peds.find(x => x.id === id); if (p2?.asTo && p2.asTo !== user.id) { sendNotif(p2.asTo, "Nuevo comentario en tarea #" + id, txt.slice(0, 80), "task", "task:" + id); } if (p2?.cId && p2.cId !== user.id && p2.cId !== p2.asTo) { sendNotif(p2.cId, "Nuevo comentario en tarea #" + id, txt.slice(0, 80), "task", "task:" + id); } const mentionRx = /@([\w\s]+?)(?=\s@|$)/g; let match; while ((match = mentionRx.exec(txt)) !== null) { const mName = match[1].trim(); const mUser = users.find((u: any) => (fn(u)).toLowerCase() === mName.toLowerCase()); if (mUser && mUser.id !== user.id && mUser.id !== p2?.asTo && mUser.id !== p2?.cId) { sendNotif(mUser.id, "Te mencionaron en tarea #" + id, txt.slice(0, 80), "task", "task:" + id); } } } catch { showT("Error al enviar mensaje", "err"); } }}
        onMonto={async (id: number, m: number) => { try { sPd(p => p.map(x => x.id === id ? { ...x, monto: m } : x)); await supabase.from("tasks").update({ amount: m }).eq("id", id); } catch (e: any) { showT(e.message || "Error", "err"); } }}
        onDel={async (id: number) => { try { sPd(p => p.filter(x => x.id !== id)); await supabase.from("tasks").delete().eq("id", id); showT("Tarea eliminada"); sSl(null); } catch (e: any) { showT(e.message || "Error", "err"); } }}
        onEditSave={async (id: number, d: any) => { try { const sd = { ...d, tit: (d.tit || "").trim(), desc: sanitize(d.desc || "") }; sPd(p => p.map(x => x.id === id ? { ...x, ...sd } : x)); await supabase.from("tasks").update({ title: sd.tit, tipo: sd.tipo, description: sd.desc, due_date: sd.fReq, urgency: sd.urg, division: sd.div || "", requires_expense: sd.rG }).eq("id", id); addLog(id, user.id, fn(user), "Editó la tarea", "sys"); showT("Tarea actualizada"); } catch (e: any) { showT(e.message || "Error", "err"); } }}
      />}
      <NotificationPanel user={user} mob={mob} shNot={shNot} sShNot={sShNot} shNotifPrefs={shNotifPrefs} sShNotifPrefs={sShNotifPrefs} computedNts={computedNts} dbNotifs={dbNotifs} ntGrouped={ntGrouped} ntColor={ntColor} unreadDb={unreadDb} badgeCount={badgeCount} notifFilter={notifFilter} sNotifFilter={sNotifFilter} notifPage={notifPage} sNotifPage={sNotifPage} notifTotal={notifTotal} NOTIF_LIMIT={NOTIF_LIMIT} refreshNotifs={refreshNotifs} pushEnabled={pushEnabled} requestPush={requestPush} getToken={getToken} sDbNotifs={sDbNotifs} sVw={sVw} sSl={sSl} sAA={sAA} sAD={sAD} peds={peds} sNavTarget={sNavTarget} />
      <CommandPalette open={cmdOpen} onClose={() => sCmdOpen(false)} items={cmdItems} />
      {toast && <Toast msg={toast.msg} type={toast.type} onDone={() => sToast(null)} />}
    </div>
    </ThemeCtx.Provider>
    </ErrorBoundary>
  );
}
